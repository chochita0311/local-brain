# FEAT-0029 Resolved Schema Object Ledger

This file is generated by `scripts/check-schema-cleanup-audit.py`. Edit the JSON decision source, not this ledger.

## Boundary And Result

- Manifest: `src/localbrain/schema-presentation.json` (`709ddc54ddb0988e7de7d068dfbc586323e179091d409704b20b7c08c7220e67`)
- Decision source: `docs/plans/evaluation/audit-0029-schema-decisions.json`
- Review state: `complete`; active migration candidates are closed and residual deferrals still require separate owner approval.
- Resolved objects: 328 = 21 tables + 244 columns + 20 explicit named indexes + 20 physical relations + 23 application relations.
- Dispositions: keep 273, change 0, remove 0, defer 55.
- SQLite FTS shadow tables and implicit uniqueness autoindexes are implementation-owned consequences, not independent application objects. Their effects are audited through the owning virtual table/UNIQUE constraint and redundant-index findings.
- Every child row inherits the complete evidence register of its `Evidence` table, while retaining an object-specific contract and rationale here.

## Candidate Decision Groups

No active change or remove candidate remains.

## Deferred Decision Groups

### canonical-timestamp-contract

- Risk: `high`
- Blocker: No durable choice yet defines canonical UTC storage, accepted legacy parser inputs, precision, timezone rendering, and preservation of mixed SQLite/source/Python timestamp text.
- Canonical backlog: [`schema-audit-timestamp-contract`](../project/backlog.md#schema-audit-timestamp-contract)

### closed-vocabulary-constraints

- Risk: `high`
- Blocker: Application constants and state transitions exist, but extensibility, invalid legacy-row handling, FTS enforcement, and safe rebuild sequencing are not approved as one durable vocabulary contract.
- Canonical backlog: [`schema-audit-closed-vocabularies`](../project/backlog.md#schema-audit-closed-vocabularies)

## Table Decisions

| Object | Decision | Risk | Evidence | Candidate / defer | Contract and rationale |
| --- | --- | --- | --- | --- | --- |
| `sources` | `keep` | `low` | [E-sources](#evidence-sources) | `—` | Required stable parent registry for every imported source domain; scan producers and cross-domain consumers depend on its IDs and kind uniqueness. |
| `source_files` | `keep` | `low` | [E-source_files](#evidence-source-files) | `—` | Required rebuildable fingerprint and retry ledger; it separates scan state from normalized descendants and drives idempotent ingestion. |
| `workspaces` | `keep` | `medium` | [E-workspaces](#evidence-workspaces) | `—` | Required stable Project identity and current browse target; SET NULL children and polymorphic links preserve user organization when discovery changes. |
| `sessions` | `keep` | `medium` | [E-sessions](#evidence-sessions) | `—` | Required normalized Session identity, parent hierarchy, activity boundary, retrieval source, and Usage Record parent under approved PRD-0002 contracts. |
| `activity_events` | `keep` | `low` | [E-activity_events](#evidence-activity-events) | `—` | The normalized event table remains required; FEAT-0032 removed its unused generic metadata slot after an all-null runtime preflight while preserving event identity, content, provenance, and ordering. |
| `usage_price_snapshots` | `keep` | `medium` | [E-usage_price_snapshots](#evidence-usage-price-snapshots) | `—` | Required immutable price provenance cited by Usage Records; RESTRICT deletion protects historical calculation evidence. |
| `usage_model_prices` | `keep` | `medium` | [E-usage_model_prices](#evidence-usage-model-prices) | `—` | Required versioned Decimal price inputs, including approved long-context tiers, under passed PRD-0004. |
| `usage_records` | `keep` | `high` | [E-usage_records](#evidence-usage-records) | `—` | The Usage Record table and frozen attribution remain required; FEAT-0031 restores the three-value compatible CHECK through backup-backed exact-value preservation without absorbing deferred timestamp metadata. |
| `context_roots` | `keep` | `high` | [E-context_roots](#evidence-context-roots) | `—` | Required non-rebuildable user source registration and enablement boundary; normalized documents cannot reconstruct this selection intent. |
| `context_documents` | `keep` | `low` | [E-context_documents](#evidence-context-documents) | `—` | The rebuildable corpus remains required and FEAT-0030 added the source and Project lookup indexes selected by the current query prefixes. |
| `workstreams` | `keep` | `high` | [E-workstreams](#evidence-workstreams) | `—` | Required top-level user-curated organization and physical parent; scans cannot recover its identity, wording, or grouping intent. |
| `threads` | `keep` | `high` | [E-threads](#evidence-threads) | `—` | Required ordered non-rebuildable work subdivision with current goals and resource ownership. |
| `workstream_links` | `keep` | `high` | [E-workstream_links](#evidence-workstream-links) | `—` | Required reviewed heterogeneous Workstream membership; application validation and historical unresolved-target behavior are intentional. |
| `thread_links` | `keep` | `high` | [E-thread_links](#evidence-thread-links) | `—` | Required reviewed heterogeneous Thread membership; inference cannot reproduce accepted scope and provenance. |
| `local_resources` | `keep` | `high` | [E-local_resources](#evidence-local-resources) | `—` | The curated resource table remains required; FEAT-0030 removed its redundant named path index while preserving the UNIQUE(path) autoindex. |
| `external_resources` | `keep` | `high` | [E-external_resources](#evidence-external-resources) | `—` | Required curated external-reference identity; URL uniqueness, title, provenance, and memberships are not source-rebuildable. |
| `suggestions` | `keep` | `high` | [E-suggestions](#evidence-suggestions) | `—` | Required generated review queue and accepted/rejected history; refresh semantics explicitly preserve protected decisions. |
| `checkpoints` | `keep` | `high` | [E-checkpoints](#evidence-checkpoints) | `—` | Confirmed snapshots remain non-rebuildable; FEAT-0030 added the Workstream/version index without transforming any snapshot row. |
| `checkpoint_resource_refs` | `keep` | `high` | [E-checkpoint_resource_refs](#evidence-checkpoint-resource-refs) | `—` | Historical resource snapshots remain required; FEAT-0030 removed the redundant named index while preserving the four-column UNIQUE autoindex. |
| `maintenance_runs` | `keep` | `high` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | Operational history and artifact correlation remain required; FEAT-0037 gives compatible databases the same optional Workstream SET NULL relation through a backup-backed, exact-value-preserving repair. |
| `search_index` | `keep` | `low` | [E-search_index](#evidence-search-index) | `—` | Required fully derived FTS projection; explicit lifecycle deletion and full rescan recovery make its application relations appropriate. |

## Column Decisions

| Object | Decision | Risk | Evidence | Candidate / defer | Contract and rationale |
| --- | --- | --- | --- | --- | --- |
| `sources.id` | `keep` | `low` | [E-sources](#evidence-sources) | `—` | `INTEGER PRIMARY KEY`; stable local registry identity and physical parent key. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `sources.kind` | `keep` | `low` | [E-sources](#evidence-sources) | `—` | `TEXT NOT NULL UNIQUE`; normalized adapter identity such as Claude, Codex, or a Local Context source kind. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `sources.name` | `keep` | `low` | [E-sources](#evidence-sources) | `—` | `TEXT NOT NULL`; user-facing source label. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `sources.root_path` | `keep` | `low` | [E-sources](#evidence-sources) | `—` | `TEXT NOT NULL`; configured local scan root or source locator; runtime values remain outside tracked docs. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `sources.enabled` | `keep` | `low` | [E-sources](#evidence-sources) | `—` | `INTEGER NOT NULL DEFAULT 1`; registry participation flag. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `sources.last_scanned_at` | `defer` | `medium` | [E-sources](#evidence-sources) | `canonical-timestamp-contract` | nullable `TEXT`; completion time of the latest source-level scan attempt. — The repository mixes source timestamps, Python ISO offsets, and SQLite CURRENT_TIMESTAMP text; a canonical storage and comparison contract is unresolved. |
| `sources.created_at` | `defer` | `medium` | [E-sources](#evidence-sources) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; registry creation time. — SQLite default timestamp format differs from application ISO timestamps. |
| `source_files.id` | `keep` | `low` | [E-source_files](#evidence-source-files) | `—` | `INTEGER PRIMARY KEY`; local scan-evidence identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `source_files.source_id` | `keep` | `low` | [E-source_files](#evidence-source-files) | `—` | `INTEGER NOT NULL` FK to `sources.id`, `ON DELETE CASCADE`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `source_files.path` | `keep` | `low` | [E-source_files](#evidence-source-files) | `—` | `TEXT NOT NULL`; source-local file identity used for scanner correlation. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `source_files.size_bytes` | `keep` | `low` | [E-source_files](#evidence-source-files) | `—` | `INTEGER NOT NULL`; fingerprint size captured at the latest attempt. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `source_files.mtime_ns` | `keep` | `low` | [E-source_files](#evidence-source-files) | `—` | `INTEGER NOT NULL`; nanosecond file modification fingerprint. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `source_files.last_scanned_at` | `defer` | `medium` | [E-source_files](#evidence-source-files) | `canonical-timestamp-contract` | `TEXT NOT NULL`; latest attempt time for this input. — Application ISO text participates in trust and freshness presentation without a durable canonical timestamp contract. |
| `source_files.status` | `defer` | `medium` | [E-source_files](#evidence-source-files) | `closed-vocabulary-constraints` | `TEXT NOT NULL DEFAULT 'ok'`; freshness state such as healthy, stale, or error. — The scanner uses ok/error/stale but no durable vocabulary and migration preflight exists yet. |
| `source_files.error` | `keep` | `low` | [E-source_files](#evidence-source-files) | `—` | nullable `TEXT`; bounded scan or parse error evidence. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `source_files.usage_contract_version` | `keep` | `low` | [E-source_files](#evidence-source-files) | `—` | nullable `TEXT`; normalizer contract used to decide whether Usage Records remain current. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `workspaces.id` | `keep` | `low` | [E-workspaces](#evidence-workspaces) | `—` | `INTEGER PRIMARY KEY`; stable local project identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `workspaces.canonical_path` | `keep` | `low` | [E-workspaces](#evidence-workspaces) | `—` | `TEXT NOT NULL UNIQUE`; normalized current workspace path. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `workspaces.display_name` | `keep` | `low` | [E-workspaces](#evidence-workspaces) | `—` | `TEXT NOT NULL`; current user-facing project label. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `workspaces.git_root` | `keep` | `low` | [E-workspaces](#evidence-workspaces) | `—` | nullable `TEXT`; current discovered repository root, not branch history. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `workspaces.exists_now` | `keep` | `low` | [E-workspaces](#evidence-workspaces) | `—` | `INTEGER NOT NULL DEFAULT 0`; current filesystem existence signal. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `workspaces.last_activity_at` | `defer` | `high` | [E-workspaces](#evidence-workspaces) | `canonical-timestamp-contract` | nullable `TEXT`; latest attributed Session activity. — Source-derived strings are compared lexically during cross-source upsert, so mixed formats can misorder activity. |
| `workspaces.created_at` | `defer` | `medium` | [E-workspaces](#evidence-workspaces) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; first registry creation. — SQLite default timestamp format differs from application ISO timestamps. |
| `workspaces.updated_at` | `defer` | `medium` | [E-workspaces](#evidence-workspaces) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; latest registry update. — Python ISO text and SQLite defaults share one column contract without normalization. |
| `sessions.id` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | `INTEGER PRIMARY KEY`; stable normalized Session identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `sessions.source_id` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | `INTEGER NOT NULL` FK to `sources.id`, `ON DELETE CASCADE`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `sessions.workspace_id` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | nullable `INTEGER` FK to `workspaces.id`, `ON DELETE SET NULL`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `sessions.external_id` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | `TEXT NOT NULL`; source-scoped Session or subsession identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `sessions.source_path` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | `TEXT NOT NULL`; authoritative native Claude or Codex JSONL path. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `sessions.cwd_raw` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | nullable `TEXT`; historical working directory exactly as normalized from source evidence. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `sessions.git_branch` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | nullable `TEXT`; branch observed in authoritative Session metadata. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `sessions.title` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | `TEXT NOT NULL`; normalized display title. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `sessions.started_at` | `defer` | `high` | [E-sessions](#evidence-sessions) | `canonical-timestamp-contract` | nullable `TEXT`; first known Session time. — Multiple source parsers supply timestamp strings that are ordered and displayed across adapters. |
| `sessions.ended_at` | `defer` | `medium` | [E-sessions](#evidence-sessions) | `canonical-timestamp-contract` | nullable `TEXT`; declared or last known end time. — Multiple source parsers supply timestamp strings without a repository-wide canonical format. |
| `sessions.last_event_at` | `defer` | `high` | [E-sessions](#evidence-sessions) | `canonical-timestamp-contract` | nullable `TEXT`; latest normalized event time. — This source-derived string drives indexes, lexical ordering, parent views, and activity attribution across adapters. |
| `sessions.event_count` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | `INTEGER NOT NULL DEFAULT 0`; normalized source event count. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `sessions.user_message_count` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | `INTEGER NOT NULL DEFAULT 0`; normalized user-message count. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `sessions.assistant_message_count` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | `INTEGER NOT NULL DEFAULT 0`; normalized assistant-message count. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `sessions.session_class` | `keep` | `medium` | [E-sessions](#evidence-sessions) | `—` | `TEXT NOT NULL DEFAULT 'work'`; checked to `work` or `maintenance`. — FEAT-0035 enforces the approved work/maintenance vocabulary in fresh and compatible databases after invalid-value preflight and exact backup-backed row preservation. |
| `sessions.session_role` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | `TEXT NOT NULL DEFAULT 'primary'`; checked to `primary` or `subsession`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `sessions.parent_external_id` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | nullable `TEXT`; source-backed parent identity retained even when unresolved. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `sessions.parent_session_id` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | nullable self-FK, `ON DELETE SET NULL`; resolved same-source parent. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `sessions.index_policy` | `keep` | `medium` | [E-sessions](#evidence-sessions) | `—` | `TEXT NOT NULL DEFAULT 'full'`; checked to `full` or `metadata_only`. — FEAT-0035 enforces the approved full/metadata_only vocabulary in fresh and compatible databases after invalid-value preflight and exact backup-backed row preservation. |
| `sessions.maintenance_run_id` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | nullable unique `TEXT` FK to `maintenance_runs.id`, `ON DELETE SET NULL`; present only for a primary metadata-only Maintenance Session. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `sessions.imported_at` | `defer` | `medium` | [E-sessions](#evidence-sessions) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; latest normalized import write time. — Python ISO values replace a SQLite-format default and lack one canonical storage rule. |
| `activity_events.id` | `keep` | `low` | [E-activity_events](#evidence-activity-events) | `—` | `TEXT PRIMARY KEY`; deterministic normalized event identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `activity_events.session_id` | `keep` | `low` | [E-activity_events](#evidence-activity-events) | `—` | `INTEGER NOT NULL` FK to `sessions.id`, `ON DELETE CASCADE`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `activity_events.sequence` | `keep` | `low` | [E-activity_events](#evidence-activity-events) | `—` | `INTEGER NOT NULL`; normalized order within the Session. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `activity_events.occurred_at` | `defer` | `high` | [E-activity_events](#evidence-activity-events) | `canonical-timestamp-contract` | nullable `TEXT`; observed event time. — Source-derived event times feed activity calculations across parser formats. |
| `activity_events.event_type` | `keep` | `low` | [E-activity_events](#evidence-activity-events) | `—` | `TEXT NOT NULL`; normalized message/tool event category. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `activity_events.role` | `keep` | `low` | [E-activity_events](#evidence-activity-events) | `—` | nullable `TEXT`; conversational role when applicable. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `activity_events.text` | `keep` | `low` | [E-activity_events](#evidence-activity-events) | `—` | nullable `TEXT`; normalized human/assistant content eligible under ingestion policy. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `activity_events.tool_name` | `keep` | `low` | [E-activity_events](#evidence-activity-events) | `—` | nullable `TEXT`; tool identity without opaque arguments/results by default. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `activity_events.source_line` | `keep` | `low` | [E-activity_events](#evidence-activity-events) | `—` | `INTEGER NOT NULL`; source evidence location. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_price_snapshots.id` | `keep` | `low` | [E-usage_price_snapshots](#evidence-usage-price-snapshots) | `—` | `TEXT PRIMARY KEY`; immutable snapshot identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_price_snapshots.label` | `keep` | `low` | [E-usage_price_snapshots](#evidence-usage-price-snapshots) | `—` | `TEXT NOT NULL`; human-readable pricing reference label. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_price_snapshots.source_ref` | `keep` | `low` | [E-usage_price_snapshots](#evidence-usage-price-snapshots) | `—` | `TEXT NOT NULL`; local provenance description for the reference data. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_price_snapshots.calculator_version` | `keep` | `low` | [E-usage_price_snapshots](#evidence-usage-price-snapshots) | `—` | `TEXT NOT NULL`; calculation contract compatible with the snapshot. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_price_snapshots.created_at` | `defer` | `medium` | [E-usage_price_snapshots](#evidence-usage-price-snapshots) | `canonical-timestamp-contract` | `TEXT NOT NULL`; fixed snapshot creation/effective evidence time. — Code-seeded timestamp text should follow the same eventual immutable timestamp contract. |
| `usage_model_prices.snapshot_id` | `keep` | `low` | [E-usage_model_prices](#evidence-usage-model-prices) | `—` | `TEXT NOT NULL`, composite PK and FK to `usage_price_snapshots.id`, `ON DELETE RESTRICT`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_model_prices.model_name` | `keep` | `low` | [E-usage_model_prices](#evidence-usage-model-prices) | `—` | `TEXT NOT NULL`, composite PK; normalized priced-model identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_model_prices.input_usd_per_million` | `keep` | `low` | [E-usage_model_prices](#evidence-usage-model-prices) | `—` | `TEXT NOT NULL`; exact decimal input rate. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_model_prices.output_usd_per_million` | `keep` | `low` | [E-usage_model_prices](#evidence-usage-model-prices) | `—` | `TEXT NOT NULL`; exact decimal output rate. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_model_prices.cache_write_usd_per_million` | `keep` | `low` | [E-usage_model_prices](#evidence-usage-model-prices) | `—` | nullable `TEXT`; exact cache-write rate when supported. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_model_prices.cache_read_usd_per_million` | `keep` | `low` | [E-usage_model_prices](#evidence-usage-model-prices) | `—` | nullable `TEXT`; exact cache-read rate when supported. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_model_prices.long_context_threshold_tokens` | `keep` | `low` | [E-usage_model_prices](#evidence-usage-model-prices) | `—` | nullable positive `INTEGER`; per-request threshold enabling long-context rates. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_model_prices.long_context_input_usd_per_million` | `keep` | `low` | [E-usage_model_prices](#evidence-usage-model-prices) | `—` | nullable `TEXT`; long-context input rate. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_model_prices.long_context_output_usd_per_million` | `keep` | `low` | [E-usage_model_prices](#evidence-usage-model-prices) | `—` | nullable `TEXT`; long-context output rate. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_model_prices.long_context_cache_write_usd_per_million` | `keep` | `low` | [E-usage_model_prices](#evidence-usage-model-prices) | `—` | nullable `TEXT`; long-context cache-write rate. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_model_prices.long_context_cache_read_usd_per_million` | `keep` | `low` | [E-usage_model_prices](#evidence-usage-model-prices) | `—` | nullable `TEXT`; long-context cache-read rate. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.id` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | `TEXT PRIMARY KEY`; deterministic normalized Usage Record identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.source_id` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | `INTEGER NOT NULL` FK to `sources.id`, `ON DELETE CASCADE`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.session_id` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | `INTEGER NOT NULL` FK to `sessions.id`, `ON DELETE CASCADE`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.source_record_id` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | `TEXT NOT NULL`; source-scoped usage observation identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.source_line` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | `INTEGER NOT NULL`; source evidence line/order. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.occurred_at` | `defer` | `high` | [E-usage_records](#evidence-usage-records) | `canonical-timestamp-contract` | nullable `TEXT`; observation time used for history when valid. — Usage period assignment parses source-derived time strings and depends on timezone-safe normalization. |
| `usage_records.raw_model` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | nullable `TEXT`; source model value retained for provenance. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.model_name` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | nullable `TEXT`; normalized model used for pricing/grouping. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.input_tokens` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | nullable nonnegative `INTEGER`; direct input token count. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.output_tokens` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | nullable nonnegative `INTEGER`; direct output token count. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.cache_write_tokens` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | nullable nonnegative `INTEGER`; cache creation/write tokens. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.cache_read_tokens` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | nullable nonnegative `INTEGER`; cache read tokens. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.reasoning_tokens` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | nullable nonnegative `INTEGER`; source-supported reasoning tokens. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.source_total_tokens` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | nullable nonnegative `INTEGER`; total reported by the source. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.total_tokens` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | nullable nonnegative `INTEGER`; normalized supported total. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.total_semantics` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | `TEXT NOT NULL`; states what the selected total represents. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.aggregation_scope` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | `TEXT NOT NULL DEFAULT 'direct'`; checked to `direct` or `includes_children`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.capability_state` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | `TEXT NOT NULL`; checked to `complete`, `partial`, or `malformed`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.capability_json` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | `TEXT NOT NULL`; versioned detail for available/missing usage components. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.calculation_state` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | `TEXT NOT NULL`; checked to `priced`, `unpriced`, `partial`, or `failed`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.estimated_cost_usd` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | nullable `TEXT`; exact decimal estimate, present only when priced. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.price_snapshot_id` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | `TEXT NOT NULL` FK to `usage_price_snapshots.id`, `ON DELETE RESTRICT`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.calculator_version` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | `TEXT NOT NULL`; cost formula/version used. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.normalizer_version` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | `TEXT NOT NULL DEFAULT 'legacy-v1'`; source normalization contract. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.calculated_at` | `defer` | `medium` | [E-usage_records](#evidence-usage-records) | `canonical-timestamp-contract` | `TEXT NOT NULL`; calculation evidence time. — Python ISO calculation evidence lacks a declared repository-wide canonical storage shape. |
| `usage_records.workspace_id_snapshot` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | nullable `INTEGER`; non-FK current browse hint captured at attribution. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.project_key` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | nullable `TEXT`; immutable grouping identity, including stable unassigned identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.project_name_snapshot` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | nullable `TEXT`; frozen display name. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.project_path_snapshot` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | nullable `TEXT`; frozen canonical-path evidence. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.project_git_root_snapshot` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | nullable `TEXT`; frozen repository-root evidence. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.attribution_basis` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | `TEXT NOT NULL DEFAULT 'unassigned'`, checked to `git_root`, `workspace_path`, or `unassigned`; compatible startup rebuilds only a legacy table missing this CHECK and preserves every stored value exactly. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `usage_records.attributed_at` | `defer` | `high` | [E-usage_records](#evidence-usage-records) | `canonical-timestamp-contract` | Fresh schema: `TEXT NOT NULL`; compatible addition is nullable `TEXT` and startup backfills existing nulls. Time the immutable Project attribution was selected. — Compatible upgrades backfill this field but do not reproduce the fresh NOT NULL contract or define a canonical format. |
| `usage_records.imported_at` | `defer` | `medium` | [E-usage_records](#evidence-usage-records) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; persistence time. — Python ISO values and SQLite defaults can coexist in historical databases. |
| `context_roots.id` | `keep` | `low` | [E-context_roots](#evidence-context-roots) | `—` | `INTEGER PRIMARY KEY`; stable Context registration identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `context_roots.path` | `keep` | `low` | [E-context_roots](#evidence-context-roots) | `—` | `TEXT NOT NULL UNIQUE`; canonical local path or source locator. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `context_roots.label` | `keep` | `low` | [E-context_roots](#evidence-context-roots) | `—` | `TEXT NOT NULL`; user-facing source label. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `context_roots.source_type` | `defer` | `high` | [E-context_roots](#evidence-context-roots) | `closed-vocabulary-constraints` | `TEXT NOT NULL DEFAULT 'folder'`; folder, file, or approved adapter type. — folder/file/apple_notes determines scan behavior; a CHECK requires explicit extensibility and legacy-row policy. |
| `context_roots.readable` | `keep` | `low` | [E-context_roots](#evidence-context-roots) | `—` | `INTEGER NOT NULL DEFAULT 1`; current source readability signal. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `context_roots.status` | `defer` | `medium` | [E-context_roots](#evidence-context-roots) | `closed-vocabulary-constraints` | `TEXT NOT NULL DEFAULT 'ready'`; ready, scanning, error, or other bounded source state. — The scanner state vocabulary is code-owned but not durably enumerated. |
| `context_roots.error` | `keep` | `low` | [E-context_roots](#evidence-context-roots) | `—` | nullable `TEXT`; latest bounded source error. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `context_roots.enabled` | `keep` | `low` | [E-context_roots](#evidence-context-roots) | `—` | `INTEGER NOT NULL DEFAULT 1`; browse/scan participation. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `context_roots.last_scanned_at` | `defer` | `medium` | [E-context_roots](#evidence-context-roots) | `canonical-timestamp-contract` | nullable `TEXT`; latest completed scan time. — Scan freshness is written in Python ISO form while other defaults use SQLite text. |
| `context_roots.created_at` | `defer` | `medium` | [E-context_roots](#evidence-context-roots) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; registration creation time. — SQLite default timestamp format differs from application ISO timestamps. |
| `context_roots.updated_at` | `defer` | `medium` | [E-context_roots](#evidence-context-roots) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; latest registry/state update. — Python ISO text and SQLite defaults share one column contract without normalization. |
| `context_documents.id` | `keep` | `low` | [E-context_documents](#evidence-context-documents) | `—` | `INTEGER PRIMARY KEY`; stable indexed-document identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `context_documents.source_id` | `keep` | `low` | [E-context_documents](#evidence-context-documents) | `—` | `INTEGER NOT NULL` FK to `sources.id`, `ON DELETE CASCADE`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `context_documents.context_root_id` | `keep` | `low` | [E-context_documents](#evidence-context-documents) | `—` | nullable `INTEGER` FK to `context_roots.id`, `ON DELETE SET NULL`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `context_documents.workspace_id` | `keep` | `low` | [E-context_documents](#evidence-context-documents) | `—` | nullable `INTEGER` FK to `workspaces.id`, `ON DELETE SET NULL`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `context_documents.path` | `keep` | `low` | [E-context_documents](#evidence-context-documents) | `—` | `TEXT NOT NULL UNIQUE`; canonical external document path/locator. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `context_documents.relative_path` | `keep` | `low` | [E-context_documents](#evidence-context-documents) | `—` | `TEXT NOT NULL`; root-relative browse path. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `context_documents.title` | `keep` | `low` | [E-context_documents](#evidence-context-documents) | `—` | `TEXT NOT NULL`; normalized display title. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `context_documents.body` | `keep` | `low` | [E-context_documents](#evidence-context-documents) | `—` | `TEXT NOT NULL`; locally indexed plaintext content. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `context_documents.content_type` | `keep` | `low` | [E-context_documents](#evidence-context-documents) | `—` | `TEXT NOT NULL DEFAULT 'text/markdown'`; normalized media/content contract. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `context_documents.size_bytes` | `keep` | `low` | [E-context_documents](#evidence-context-documents) | `—` | `INTEGER NOT NULL`; source size fingerprint. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `context_documents.mtime_ns` | `keep` | `low` | [E-context_documents](#evidence-context-documents) | `—` | `INTEGER NOT NULL`; nanosecond source modification fingerprint. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `context_documents.content_hash` | `keep` | `low` | [E-context_documents](#evidence-context-documents) | `—` | `TEXT NOT NULL`; indexed content fingerprint. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `context_documents.imported_at` | `defer` | `medium` | [E-context_documents](#evidence-context-documents) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; latest indexed write time. — Scanner writes Python ISO text while fresh defaults use SQLite format. |
| `workstreams.id` | `keep` | `low` | [E-workstreams](#evidence-workstreams) | `—` | `INTEGER PRIMARY KEY`; stable Workstream identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `workstreams.name` | `keep` | `low` | [E-workstreams](#evidence-workstreams) | `—` | `TEXT NOT NULL UNIQUE`; user-owned top-level name. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `workstreams.status` | `defer` | `high` | [E-workstreams](#evidence-workstreams) | `closed-vocabulary-constraints` | `TEXT NOT NULL DEFAULT 'active'`; application-bounded active, paused, done, or archived state. — Application validation defines active/paused/done/archived, but non-rebuildable rows lack database enforcement. |
| `workstreams.summary` | `keep` | `low` | [E-workstreams](#evidence-workstreams) | `—` | nullable `TEXT`; user-maintained purpose/context. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `workstreams.last_activity_at` | `defer` | `high` | [E-workstreams](#evidence-workstreams) | `canonical-timestamp-contract` | nullable `TEXT`; latest linked/user organization activity signal. — Latest activity takes MAX across source ISO, SQLite datetime(), and resource update formats, making lexical consistency material. |
| `workstreams.created_at` | `defer` | `medium` | [E-workstreams](#evidence-workstreams) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; creation time. — SQLite default timestamp format differs from application ISO timestamps. |
| `workstreams.updated_at` | `defer` | `medium` | [E-workstreams](#evidence-workstreams) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; latest mutation time. — Python ISO values participate in ordering beside SQLite-format historical defaults. |
| `threads.id` | `keep` | `low` | [E-threads](#evidence-threads) | `—` | `INTEGER PRIMARY KEY`; stable Thread identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `threads.workstream_id` | `keep` | `low` | [E-threads](#evidence-threads) | `—` | `INTEGER NOT NULL` FK to `workstreams.id`, `ON DELETE CASCADE`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `threads.title` | `keep` | `low` | [E-threads](#evidence-threads) | `—` | `TEXT NOT NULL`; title unique within its Workstream. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `threads.status` | `defer` | `high` | [E-threads](#evidence-threads) | `closed-vocabulary-constraints` | `TEXT NOT NULL DEFAULT 'active'`; application-bounded active, blocked, paused, or done state. — Application validation defines active/blocked/paused/done, but non-rebuildable rows lack database enforcement. |
| `threads.summary` | `keep` | `low` | [E-threads](#evidence-threads) | `—` | nullable `TEXT`; current Thread context. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `threads.current_goal` | `keep` | `low` | [E-threads](#evidence-threads) | `—` | nullable `TEXT`; current intended outcome. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `threads.next_action` | `keep` | `low` | [E-threads](#evidence-threads) | `—` | nullable `TEXT`; next executable step. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `threads.last_activity_at` | `defer` | `medium` | [E-threads](#evidence-threads) | `canonical-timestamp-contract` | nullable `TEXT`; latest Thread-scoped activity signal. — Nullable activity text is part of the same mixed-format ordering domain. |
| `threads.position` | `keep` | `low` | [E-threads](#evidence-threads) | `—` | `INTEGER NOT NULL DEFAULT 0`; user-facing order within the Workstream. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `threads.created_at` | `defer` | `medium` | [E-threads](#evidence-threads) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; creation time. — SQLite default timestamp format differs from application ISO timestamps. |
| `threads.updated_at` | `defer` | `medium` | [E-threads](#evidence-threads) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; latest mutation time. — Python ISO values participate in Workstream detail ordering and freshness. |
| `workstream_links.id` | `keep` | `low` | [E-workstream_links](#evidence-workstream-links) | `—` | `INTEGER PRIMARY KEY`; link identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `workstream_links.workstream_id` | `keep` | `low` | [E-workstream_links](#evidence-workstream-links) | `—` | `INTEGER NOT NULL` FK to `workstreams.id`, `ON DELETE CASCADE`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `workstream_links.entity_type` | `defer` | `high` | [E-workstream_links](#evidence-workstream-links) | `closed-vocabulary-constraints` | `TEXT NOT NULL`; application-enforced target discriminator. — Application validation uses a five-type set, while a constraint needs an approved extensibility path for future resources. |
| `workstream_links.entity_id` | `keep` | `low` | [E-workstream_links](#evidence-workstream-links) | `—` | `TEXT NOT NULL`; target identity interpreted by `entity_type`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `workstream_links.relation_type` | `keep` | `low` | [E-workstream_links](#evidence-workstream-links) | `—` | `TEXT NOT NULL DEFAULT 'related-to'`; user/reviewed relation meaning. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `workstream_links.confidence` | `keep` | `low` | [E-workstream_links](#evidence-workstream-links) | `—` | nullable `REAL`; generated-link confidence when applicable. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `workstream_links.linked_by` | `keep` | `low` | [E-workstream_links](#evidence-workstream-links) | `—` | `TEXT NOT NULL DEFAULT 'user'`; user or generator provenance. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `workstream_links.created_at` | `defer` | `medium` | [E-workstream_links](#evidence-workstream-links) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; link creation time. — Link chronology should share a canonical format before any timestamp migration. |
| `thread_links.id` | `keep` | `low` | [E-thread_links](#evidence-thread-links) | `—` | `INTEGER PRIMARY KEY`; link identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `thread_links.thread_id` | `keep` | `low` | [E-thread_links](#evidence-thread-links) | `—` | `INTEGER NOT NULL` FK to `threads.id`, `ON DELETE CASCADE`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `thread_links.entity_type` | `defer` | `high` | [E-thread_links](#evidence-thread-links) | `closed-vocabulary-constraints` | `TEXT NOT NULL`; application-enforced target discriminator. — Application validation uses a five-type set, while a constraint needs an approved extensibility path for future resources. |
| `thread_links.entity_id` | `keep` | `low` | [E-thread_links](#evidence-thread-links) | `—` | `TEXT NOT NULL`; target identity interpreted by `entity_type`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `thread_links.relation_type` | `keep` | `low` | [E-thread_links](#evidence-thread-links) | `—` | `TEXT NOT NULL DEFAULT 'related-to'`; relation meaning. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `thread_links.confidence` | `keep` | `low` | [E-thread_links](#evidence-thread-links) | `—` | nullable `REAL`; generated-link confidence. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `thread_links.linked_by` | `keep` | `low` | [E-thread_links](#evidence-thread-links) | `—` | `TEXT NOT NULL DEFAULT 'user'`; origin provenance. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `thread_links.created_at` | `defer` | `medium` | [E-thread_links](#evidence-thread-links) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; link creation time. — Link chronology should share a canonical format before any timestamp migration. |
| `local_resources.id` | `keep` | `low` | [E-local_resources](#evidence-local-resources) | `—` | `INTEGER PRIMARY KEY`; canonical local-resource identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `local_resources.path` | `keep` | `low` | [E-local_resources](#evidence-local-resources) | `—` | `TEXT NOT NULL UNIQUE`; canonical local locator. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `local_resources.resource_type` | `keep` | `low` | [E-local_resources](#evidence-local-resources) | `—` | `TEXT NOT NULL DEFAULT 'path'`; repository, directory, file, or generic path category. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `local_resources.title` | `keep` | `low` | [E-local_resources](#evidence-local-resources) | `—` | `TEXT NOT NULL`; user-facing name. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `local_resources.summary` | `keep` | `low` | [E-local_resources](#evidence-local-resources) | `—` | nullable `TEXT`; user/reviewed description. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `local_resources.exists_now` | `keep` | `low` | [E-local_resources](#evidence-local-resources) | `—` | `INTEGER NOT NULL DEFAULT 0`; current existence signal. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `local_resources.discovered_by` | `keep` | `low` | [E-local_resources](#evidence-local-resources) | `—` | `TEXT NOT NULL DEFAULT 'user'`; registration provenance. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `local_resources.created_at` | `defer` | `medium` | [E-local_resources](#evidence-local-resources) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; creation time. — SQLite default timestamp format differs from application ISO timestamps. |
| `local_resources.updated_at` | `defer` | `medium` | [E-local_resources](#evidence-local-resources) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; latest metadata/existence update. — Resource freshness participates in cross-domain latest-activity calculation. |
| `external_resources.id` | `keep` | `low` | [E-external_resources](#evidence-external-resources) | `—` | `INTEGER PRIMARY KEY`; canonical external-resource identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `external_resources.resource_type` | `keep` | `low` | [E-external_resources](#evidence-external-resources) | `—` | `TEXT NOT NULL`; application-bounded Jira, Wiki, Slack, Git, document, or URL type. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `external_resources.title` | `keep` | `low` | [E-external_resources](#evidence-external-resources) | `—` | `TEXT NOT NULL`; user-facing label. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `external_resources.url` | `keep` | `low` | [E-external_resources](#evidence-external-resources) | `—` | `TEXT NOT NULL UNIQUE`; canonical external locator. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `external_resources.summary` | `keep` | `low` | [E-external_resources](#evidence-external-resources) | `—` | nullable `TEXT`; user/reviewed description. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `external_resources.source_role` | `keep` | `low` | [E-external_resources](#evidence-external-resources) | `—` | nullable `TEXT`; role of the reference in the current work context. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `external_resources.created_at` | `defer` | `medium` | [E-external_resources](#evidence-external-resources) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; creation time. — SQLite default timestamp format differs from application ISO timestamps. |
| `external_resources.updated_at` | `defer` | `medium` | [E-external_resources](#evidence-external-resources) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; latest metadata update. — Resource freshness participates in cross-domain latest-activity calculation. |
| `suggestions.id` | `keep` | `low` | [E-suggestions](#evidence-suggestions) | `—` | `INTEGER PRIMARY KEY`; review-item identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `suggestions.suggestion_type` | `keep` | `low` | [E-suggestions](#evidence-suggestions) | `—` | `TEXT NOT NULL`; proposed operation category. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `suggestions.target_type` | `defer` | `high` | [E-suggestions](#evidence-suggestions) | `closed-vocabulary-constraints` | `TEXT NOT NULL`; application target discriminator, normally Workstream or Thread. — Current workstream/thread targets form a closed application state but lack a durable migration-safe constraint. |
| `suggestions.target_id` | `keep` | `low` | [E-suggestions](#evidence-suggestions) | `—` | `INTEGER NOT NULL`; application target identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `suggestions.title` | `keep` | `low` | [E-suggestions](#evidence-suggestions) | `—` | `TEXT NOT NULL`; concise proposal label. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `suggestions.description` | `keep` | `low` | [E-suggestions](#evidence-suggestions) | `—` | nullable `TEXT`; proposed change detail. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `suggestions.rationale` | `keep` | `low` | [E-suggestions](#evidence-suggestions) | `—` | nullable `TEXT`; human-readable evidence/reasoning. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `suggestions.payload_json` | `keep` | `low` | [E-suggestions](#evidence-suggestions) | `—` | nullable `TEXT`; structured proposal payload applied only after validation/acceptance. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `suggestions.fingerprint` | `keep` | `low` | [E-suggestions](#evidence-suggestions) | `—` | `TEXT NOT NULL UNIQUE`; stable deduplication identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `suggestions.origin_run_id` | `keep` | `low` | [E-suggestions](#evidence-suggestions) | `—` | nullable `TEXT`; non-FK Task Runner provenance. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `suggestions.status` | `defer` | `high` | [E-suggestions](#evidence-suggestions) | `closed-vocabulary-constraints` | `TEXT NOT NULL DEFAULT 'pending'`; explicit review state. — Pending, accepted, rejected, and superseded are non-rebuildable review states and require an explicit durable vocabulary. |
| `suggestions.confidence` | `keep` | `low` | [E-suggestions](#evidence-suggestions) | `—` | nullable `REAL`; bounded generator confidence. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `suggestions.created_at` | `defer` | `medium` | [E-suggestions](#evidence-suggestions) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; proposal creation time. — Queue ordering mixes default and application-written time shapes. |
| `suggestions.resolved_at` | `defer` | `medium` | [E-suggestions](#evidence-suggestions) | `canonical-timestamp-contract` | nullable `TEXT`; acceptance/rejection time. — Review chronology is non-rebuildable and needs preservation-aware normalization. |
| `checkpoints.id` | `keep` | `low` | [E-checkpoints](#evidence-checkpoints) | `—` | `INTEGER PRIMARY KEY`; checkpoint identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `checkpoints.workstream_id` | `keep` | `low` | [E-checkpoints](#evidence-checkpoints) | `—` | `INTEGER NOT NULL` FK to `workstreams.id`, `ON DELETE CASCADE`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `checkpoints.current_goal` | `keep` | `low` | [E-checkpoints](#evidence-checkpoints) | `—` | nullable `TEXT`; confirmed goal at snapshot time. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `checkpoints.confirmed_facts` | `keep` | `low` | [E-checkpoints](#evidence-checkpoints) | `—` | nullable `TEXT`; confirmed factual context. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `checkpoints.recent_decisions` | `keep` | `low` | [E-checkpoints](#evidence-checkpoints) | `—` | nullable `TEXT`; decisions that govern resumption. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `checkpoints.open_questions` | `keep` | `low` | [E-checkpoints](#evidence-checkpoints) | `—` | nullable `TEXT`; unresolved questions. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `checkpoints.next_actions` | `keep` | `low` | [E-checkpoints](#evidence-checkpoints) | `—` | nullable `TEXT`; confirmed subsequent steps. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `checkpoints.files_to_open` | `keep` | `low` | [E-checkpoints](#evidence-checkpoints) | `—` | nullable `TEXT`; explicit resume file hints. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `checkpoints.confirmed_at` | `defer` | `high` | [E-checkpoints](#evidence-checkpoints) | `canonical-timestamp-contract` | nullable `TEXT`; human confirmation time. — Human-confirmed chronology is non-rebuildable and must not be rewritten without a canonical parser and backup. |
| `checkpoints.based_on_activity_at` | `defer` | `high` | [E-checkpoints](#evidence-checkpoints) | `canonical-timestamp-contract` | nullable `TEXT`; latest linked activity included in the summary. — This boundary can originate from mixed cross-domain activity formats and is semantically part of the confirmed snapshot. |
| `checkpoints.version` | `keep` | `low` | [E-checkpoints](#evidence-checkpoints) | `—` | `INTEGER NOT NULL DEFAULT 1`; monotonic number within the Workstream. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `checkpoints.updated_at` | `defer` | `high` | [E-checkpoints](#evidence-checkpoints) | `canonical-timestamp-contract` | Fresh schema: `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; compatible addition is nullable `TEXT`, then existing nulls are backfilled from `created_at`. Latest compatible/update time. — Fresh DDL is NOT NULL with default, compatible upgrades add nullable TEXT and backfill once; normalization and constraint repair must be preservation-safe. |
| `checkpoints.created_at` | `defer` | `high` | [E-checkpoints](#evidence-checkpoints) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; snapshot creation time. — Confirmed history may contain SQLite-format defaults and must be preserved exactly until the timestamp contract is approved. |
| `checkpoint_resource_refs.id` | `keep` | `low` | [E-checkpoint_resource_refs](#evidence-checkpoint-resource-refs) | `—` | `INTEGER PRIMARY KEY`; snapshot-reference identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `checkpoint_resource_refs.checkpoint_id` | `keep` | `low` | [E-checkpoint_resource_refs](#evidence-checkpoint-resource-refs) | `—` | `INTEGER NOT NULL` FK to `checkpoints.id`, `ON DELETE CASCADE`. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `checkpoint_resource_refs.thread_id` | `keep` | `low` | [E-checkpoint_resource_refs](#evidence-checkpoint-resource-refs) | `—` | nullable `INTEGER` FK to `threads.id`, `ON DELETE SET NULL`; null denotes Workstream-level or removed Thread context. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `checkpoint_resource_refs.entity_type` | `defer` | `high` | [E-checkpoint_resource_refs](#evidence-checkpoint-resource-refs) | `closed-vocabulary-constraints` | `TEXT NOT NULL`; snapshotted polymorphic target type. — Immutable snapshot targets should align with link entity types, but legacy preservation and future resource expansion remain unresolved. |
| `checkpoint_resource_refs.entity_id` | `keep` | `low` | [E-checkpoint_resource_refs](#evidence-checkpoint-resource-refs) | `—` | `TEXT NOT NULL`; target identity at checkpoint time. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `checkpoint_resource_refs.relation_type` | `keep` | `low` | [E-checkpoint_resource_refs](#evidence-checkpoint-resource-refs) | `—` | `TEXT NOT NULL DEFAULT 'evidence'`; captured relation meaning. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `checkpoint_resource_refs.created_at` | `defer` | `high` | [E-checkpoint_resource_refs](#evidence-checkpoint-resource-refs) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; capture time. — Historical snapshot membership chronology is non-rebuildable. |
| `maintenance_runs.id` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | `TEXT PRIMARY KEY`; stable local Run identity. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.workstream_id` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | nullable `INTEGER` FK to `workstreams.id`, `ON DELETE SET NULL`; identifies the Workstream from which the maintenance execution was started. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.task_type` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | nullable `TEXT`; bounded maintenance operation category. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.runner` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | `TEXT NOT NULL DEFAULT 'claude'`; selected local runner implementation. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.cwd` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | nullable `TEXT`; local working directory used for execution. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.status` | `defer` | `high` | [E-maintenance_runs](#evidence-maintenance-runs) | `closed-vocabulary-constraints` | `TEXT NOT NULL DEFAULT 'prepared'`; durable lifecycle state. — Prepared/queued/running/cancelling and terminal values form a state machine that is not yet durably enumerated. |
| `maintenance_runs.pid` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | nullable `INTEGER`; active local process identity, never sufficient alone for ownership after restart. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.started_at` | `defer` | `medium` | [E-maintenance_runs](#evidence-maintenance-runs) | `canonical-timestamp-contract` | nullable `TEXT`; process start time. — Operational state transitions use Python ISO values beside database defaults. |
| `maintenance_runs.completed_at` | `defer` | `medium` | [E-maintenance_runs](#evidence-maintenance-runs) | `canonical-timestamp-contract` | nullable `TEXT`; terminal completion time. — Operational ordering and restart reconciliation require canonical comparable values. |
| `maintenance_runs.source_snapshot_json` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | nullable `TEXT`; bounded source-selection snapshot. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.manifest_path` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | nullable `TEXT`; private runtime reference manifest path. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.prompt_path` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | nullable `TEXT`; private runtime prompt artifact path. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.stream_path` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | nullable `TEXT`; private runtime stream artifact path. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.result_path` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | nullable `TEXT`; private runtime result artifact path. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.stderr_path` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | nullable `TEXT`; private runtime stderr artifact path. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.structured_result_json` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | nullable `TEXT`; validated structured result retained for review. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.suggestions_created` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | `INTEGER NOT NULL DEFAULT 0`; accepted parse output count, not acceptance count. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.refresh_suggestions` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | `INTEGER NOT NULL DEFAULT 0`; whether eligible pending suggestions were refreshed. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.mcp_call_budget` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | `INTEGER NOT NULL DEFAULT 20`; advisory call ceiling for the Run. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.mcp_calls_used` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | `INTEGER NOT NULL DEFAULT 0`; observed call count. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.mcp_tool_calls_json` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | nullable `TEXT`; bounded tool-call accounting. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.mcp_budget_exceeded` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | `INTEGER NOT NULL DEFAULT 0`; recorded budget breach signal. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.summary` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | nullable `TEXT`; terminal human-readable outcome. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.error` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | nullable `TEXT`; bounded failure evidence. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `maintenance_runs.updated_at` | `defer` | `high` | [E-maintenance_runs](#evidence-maintenance-runs) | `canonical-timestamp-contract` | Fresh schema: `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; compatible addition is nullable `TEXT`, then existing nulls are backfilled from `created_at`. Latest state transition time. — Fresh DDL is NOT NULL with default, compatible upgrades add nullable TEXT and backfill once; repair should share the timestamp migration boundary. |
| `maintenance_runs.created_at` | `defer` | `medium` | [E-maintenance_runs](#evidence-maintenance-runs) | `canonical-timestamp-contract` | `TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`; preparation time. — Run list ordering can span SQLite-format defaults and Python ISO timestamps. |
| `search_index.entity_type` | `defer` | `low` | [E-search_index](#evidence-search-index) | `closed-vocabulary-constraints` | FTS5 `UNINDEXED`; application discriminator for `session` or `document`. — The projection currently supports session/document only; an FTS-compatible enforcement mechanism and expansion rule remain unresolved. |
| `search_index.entity_id` | `keep` | `low` | [E-search_index](#evidence-search-index) | `—` | FTS5 `UNINDEXED`; text identity of the projected source object. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `search_index.source_kind` | `keep` | `low` | [E-search_index](#evidence-search-index) | `—` | FTS5 `UNINDEXED`; normalized source provenance used by consumers. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `search_index.title` | `keep` | `low` | [E-search_index](#evidence-search-index) | `—` | indexed FTS5 text; display/search title. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `search_index.body` | `keep` | `low` | [E-search_index](#evidence-search-index) | `—` | indexed FTS5 text; eligible message/document body. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |
| `search_index.path` | `keep` | `low` | [E-search_index](#evidence-search-index) | `—` | indexed FTS5 text; bounded source or relative-path search field. — Retain the documented semantic contract and known table producer/consumer boundary; no contradictory ownership, duplication, or recovery evidence was found. |

## Index Decisions

| Object | Decision | Risk | Evidence | Candidate / defer | Contract and rationale |
| --- | --- | --- | --- | --- | --- |
| `sessions.idx_sessions_class` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | session_class, last_event_at DESC — Retain the named index because its leading columns support a current filter, join, or ordering path and no covering autoindex proves it redundant. |
| `sessions.idx_sessions_last_event` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | last_event_at DESC — Retain the named index because its leading columns support a current filter, join, or ordering path and no covering autoindex proves it redundant. |
| `sessions.idx_sessions_parent` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | parent_session_id, last_event_at DESC — Retain the named index because its leading columns support a current filter, join, or ordering path and no covering autoindex proves it redundant. |
| `sessions.idx_sessions_role` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | session_role, last_event_at DESC — Retain the named index because its leading columns support a current filter, join, or ordering path and no covering autoindex proves it redundant. |
| `sessions.idx_sessions_workspace` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | workspace_id, last_event_at DESC — Retain the named index because its leading columns support a current filter, join, or ordering path and no covering autoindex proves it redundant. |
| `usage_records.idx_usage_records_model_time` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | model_name, occurred_at — Retain the named index because its leading columns support a current filter, join, or ordering path and no covering autoindex proves it redundant. |
| `usage_records.idx_usage_records_session_time` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | session_id, occurred_at — Retain the named index because its leading columns support a current filter, join, or ordering path and no covering autoindex proves it redundant. |
| `usage_records.idx_usage_records_source_time` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | source_id, occurred_at — Retain the named index because its leading columns support a current filter, join, or ordering path and no covering autoindex proves it redundant. |
| `context_documents.idx_documents_context_root` | `keep` | `low` | [E-context_documents](#evidence-context-documents) | `—` | context_root_id, mtime_ns DESC — Retain the named index because its leading columns support a current filter, join, or ordering path and no covering autoindex proves it redundant. |
| `context_documents.idx_documents_mtime` | `keep` | `low` | [E-context_documents](#evidence-context-documents) | `—` | mtime_ns DESC — Retain the named index because its leading columns support a current filter, join, or ordering path and no covering autoindex proves it redundant. |
| `context_documents.idx_documents_source` | `keep` | `low` | [E-context_documents](#evidence-context-documents) | `—` | source_id — Retain the named index because its leading columns support a current filter, join, or ordering path and no covering autoindex proves it redundant. |
| `context_documents.idx_documents_workspace` | `keep` | `low` | [E-context_documents](#evidence-context-documents) | `—` | workspace_id, mtime_ns DESC — Retain the named index because its leading columns support a current filter, join, or ordering path and no covering autoindex proves it redundant. |
| `threads.idx_threads_workstream` | `keep` | `low` | [E-threads](#evidence-threads) | `—` | workstream_id, status, position — Retain the named index because its leading columns support a current filter, join, or ordering path and no covering autoindex proves it redundant. |
| `workstream_links.idx_workstream_links_entity` | `keep` | `low` | [E-workstream_links](#evidence-workstream-links) | `—` | entity_type, entity_id — Retain the named index because its leading columns support a current filter, join, or ordering path and no covering autoindex proves it redundant. |
| `thread_links.idx_thread_links_entity` | `keep` | `low` | [E-thread_links](#evidence-thread-links) | `—` | entity_type, entity_id — Retain the named index because its leading columns support a current filter, join, or ordering path and no covering autoindex proves it redundant. |
| `suggestions.idx_suggestions_origin_run` | `keep` | `low` | [E-suggestions](#evidence-suggestions) | `—` | origin_run_id, status — Retain the named index because its leading columns support a current filter, join, or ordering path and no covering autoindex proves it redundant. |
| `suggestions.idx_suggestions_target` | `keep` | `low` | [E-suggestions](#evidence-suggestions) | `—` | target_type, target_id, status — Retain the named index because its leading columns support a current filter, join, or ordering path and no covering autoindex proves it redundant. |
| `checkpoints.idx_checkpoints_workstream_version` | `keep` | `low` | [E-checkpoints](#evidence-checkpoints) | `—` | workstream_id, version DESC — Retain the named index because its leading columns support a current filter, join, or ordering path and no covering autoindex proves it redundant. |
| `maintenance_runs.idx_maintenance_runs_status` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | status, updated_at DESC — Retain the named index because its leading columns support a current filter, join, or ordering path and no covering autoindex proves it redundant. |
| `maintenance_runs.idx_maintenance_runs_workstream` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | workstream_id, created_at DESC — Retain the named index because its leading columns support a current filter, join, or ordering path and no covering autoindex proves it redundant. |

## Physical Relation Decisions

| Object | Decision | Risk | Evidence | Candidate / defer | Contract and rationale |
| --- | --- | --- | --- | --- | --- |
| `activity_events(session_id)->sessions(id)` | `keep` | `low` | [E-activity_events](#evidence-activity-events) | `—` | sqlite-foreign-key; ON DELETE CASCADE — Retain the physical relation because it enforces the documented ownership and deletion contract for current databases. |
| `checkpoint_resource_refs(checkpoint_id)->checkpoints(id)` | `keep` | `low` | [E-checkpoint_resource_refs](#evidence-checkpoint-resource-refs) | `—` | sqlite-foreign-key; ON DELETE CASCADE — Retain the physical relation because it enforces the documented ownership and deletion contract for current databases. |
| `checkpoint_resource_refs(thread_id)->threads(id)` | `keep` | `low` | [E-checkpoint_resource_refs](#evidence-checkpoint-resource-refs) | `—` | sqlite-foreign-key; ON DELETE SET NULL — Retain the physical relation because it enforces the documented ownership and deletion contract for current databases. |
| `checkpoints(workstream_id)->workstreams(id)` | `keep` | `low` | [E-checkpoints](#evidence-checkpoints) | `—` | sqlite-foreign-key; ON DELETE CASCADE — Retain the physical relation because it enforces the documented ownership and deletion contract for current databases. |
| `context_documents(context_root_id)->context_roots(id)` | `keep` | `low` | [E-context_documents](#evidence-context-documents) | `—` | sqlite-foreign-key; ON DELETE SET NULL — Retain the physical relation because it enforces the documented ownership and deletion contract for current databases. |
| `context_documents(source_id)->sources(id)` | `keep` | `low` | [E-context_documents](#evidence-context-documents) | `—` | sqlite-foreign-key; ON DELETE CASCADE — Retain the physical relation because it enforces the documented ownership and deletion contract for current databases. |
| `context_documents(workspace_id)->workspaces(id)` | `keep` | `low` | [E-context_documents](#evidence-context-documents) | `—` | sqlite-foreign-key; ON DELETE SET NULL — Retain the physical relation because it enforces the documented ownership and deletion contract for current databases. |
| `maintenance_runs(workstream_id)->workstreams(id)` | `keep` | `low` | [E-maintenance_runs](#evidence-maintenance-runs) | `—` | sqlite-foreign-key; ON DELETE SET NULL — Retain the physical relation because it enforces the documented ownership and deletion contract for current databases. |
| `sessions(maintenance_run_id)->maintenance_runs(id)` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | sqlite-foreign-key; ON DELETE SET NULL — Retain the physical relation because it enforces the documented ownership and deletion contract for current databases. |
| `sessions(parent_session_id)->sessions(id)` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | sqlite-foreign-key; ON DELETE SET NULL — Retain the physical relation because it enforces the documented ownership and deletion contract for current databases. |
| `sessions(source_id)->sources(id)` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | sqlite-foreign-key; ON DELETE CASCADE — Retain the physical relation because it enforces the documented ownership and deletion contract for current databases. |
| `sessions(workspace_id)->workspaces(id)` | `keep` | `low` | [E-sessions](#evidence-sessions) | `—` | sqlite-foreign-key; ON DELETE SET NULL — Retain the physical relation because it enforces the documented ownership and deletion contract for current databases. |
| `source_files(source_id)->sources(id)` | `keep` | `low` | [E-source_files](#evidence-source-files) | `—` | sqlite-foreign-key; ON DELETE CASCADE — Retain the physical relation because it enforces the documented ownership and deletion contract for current databases. |
| `thread_links(thread_id)->threads(id)` | `keep` | `low` | [E-thread_links](#evidence-thread-links) | `—` | sqlite-foreign-key; ON DELETE CASCADE — Retain the physical relation because it enforces the documented ownership and deletion contract for current databases. |
| `threads(workstream_id)->workstreams(id)` | `keep` | `low` | [E-threads](#evidence-threads) | `—` | sqlite-foreign-key; ON DELETE CASCADE — Retain the physical relation because it enforces the documented ownership and deletion contract for current databases. |
| `usage_model_prices(snapshot_id)->usage_price_snapshots(id)` | `keep` | `low` | [E-usage_model_prices](#evidence-usage-model-prices) | `—` | sqlite-foreign-key; ON DELETE RESTRICT — Retain the physical relation because it enforces the documented ownership and deletion contract for current databases. |
| `usage_records(session_id)->sessions(id)` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | sqlite-foreign-key; ON DELETE CASCADE — Retain the physical relation because it enforces the documented ownership and deletion contract for current databases. |
| `usage_records(source_id)->sources(id)` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | sqlite-foreign-key; ON DELETE CASCADE — Retain the physical relation because it enforces the documented ownership and deletion contract for current databases. |
| `usage_records(price_snapshot_id)->usage_price_snapshots(id)` | `keep` | `low` | [E-usage_records](#evidence-usage-records) | `—` | sqlite-foreign-key; ON DELETE RESTRICT — Retain the physical relation because it enforces the documented ownership and deletion contract for current databases. |
| `workstream_links(workstream_id)->workstreams(id)` | `keep` | `low` | [E-workstream_links](#evidence-workstream-links) | `—` | sqlite-foreign-key; ON DELETE CASCADE — Retain the physical relation because it enforces the documented ownership and deletion contract for current databases. |

## Application Relation Decisions

| Object | Decision | Risk | Evidence | Candidate / defer | Contract and rationale |
| --- | --- | --- | --- | --- | --- |
| `checkpoint_resource_refs->context_documents [app snapshot target]` | `keep` | `low` | [E-checkpoint_resource_refs](#evidence-checkpoint-resource-refs) | `—` | app snapshot target — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `checkpoint_resource_refs->external_resources [app snapshot target]` | `keep` | `low` | [E-checkpoint_resource_refs](#evidence-checkpoint-resource-refs) | `—` | app snapshot target — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `checkpoint_resource_refs->local_resources [app snapshot target]` | `keep` | `low` | [E-checkpoint_resource_refs](#evidence-checkpoint-resource-refs) | `—` | app snapshot target — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `checkpoint_resource_refs->sessions [app snapshot target]` | `keep` | `low` | [E-checkpoint_resource_refs](#evidence-checkpoint-resource-refs) | `—` | app snapshot target — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `checkpoint_resource_refs->workspaces [app snapshot target]` | `keep` | `low` | [E-checkpoint_resource_refs](#evidence-checkpoint-resource-refs) | `—` | app snapshot target — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `search_index->context_documents [app projection]` | `keep` | `low` | [E-search_index](#evidence-search-index) | `—` | app projection — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `search_index->sessions [app projection]` | `keep` | `low` | [E-search_index](#evidence-search-index) | `—` | app projection — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `source_files->context_documents [app path]` | `keep` | `low` | [E-source_files](#evidence-source-files) | `—` | app path — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `source_files->sessions [app source_path]` | `keep` | `low` | [E-source_files](#evidence-source-files) | `—` | app source_path — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `suggestions->maintenance_runs [app origin]` | `keep` | `low` | [E-suggestions](#evidence-suggestions) | `—` | app origin — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `suggestions->threads [app target]` | `keep` | `low` | [E-suggestions](#evidence-suggestions) | `—` | app target — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `suggestions->workstreams [app target]` | `keep` | `low` | [E-suggestions](#evidence-suggestions) | `—` | app target — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `thread_links->context_documents [app document target]` | `keep` | `low` | [E-thread_links](#evidence-thread-links) | `—` | app document target — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `thread_links->external_resources [app external target]` | `keep` | `low` | [E-thread_links](#evidence-thread-links) | `—` | app external target — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `thread_links->local_resources [app local target]` | `keep` | `low` | [E-thread_links](#evidence-thread-links) | `—` | app local target — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `thread_links->sessions [app session target]` | `keep` | `low` | [E-thread_links](#evidence-thread-links) | `—` | app session target — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `thread_links->workspaces [app project target]` | `keep` | `low` | [E-thread_links](#evidence-thread-links) | `—` | app project target — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `workspaces->usage_records [app frozen snapshot]` | `keep` | `low` | [E-workspaces](#evidence-workspaces) | `—` | app frozen snapshot — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `workstream_links->context_documents [app document target]` | `keep` | `low` | [E-workstream_links](#evidence-workstream-links) | `—` | app document target — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `workstream_links->external_resources [app external target]` | `keep` | `low` | [E-workstream_links](#evidence-workstream-links) | `—` | app external target — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `workstream_links->local_resources [app local target]` | `keep` | `low` | [E-workstream_links](#evidence-workstream-links) | `—` | app local target — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `workstream_links->sessions [app session target]` | `keep` | `low` | [E-workstream_links](#evidence-workstream-links) | `—` | app session target — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |
| `workstream_links->workspaces [app project target]` | `keep` | `low` | [E-workstream_links](#evidence-workstream-links) | `—` | app project target — Retain the application relation because polymorphic, projection, snapshot, or source-path identity intentionally cannot use one ordinary SQLite foreign key and current producers apply its lifecycle rule. |

## Evidence Register

### Evidence: sources

<a id="evidence-sources"></a>

- Subject owner: `source-registry-and-scans`; current semantic owner: `docs/policies/project/data-model/source-registry-and-scans.md`.
- Fresh / compatible source: complete fresh definition in `schema.sql`; no compatible columns or explicit named indexes in `db.py`.
- Purpose and authority: one application-managed registry row per source kind. Configured Claude, Codex, and Local Context source roots are authoritative; `ingest/scanner.py` upserts their current registry representation.
- Producers: `ingest/scanner.py`; `db.py` reads kinds when migration changes must stale Session inputs.
- Consumers: `queries.py`, `activity.py`, `retrieval.py`, `usage_queries.py`, `workstreams.py`, `runner.py`, `main.py`, and CLI scan reporting.
- Lifecycle: Source-derived and rebuildable — source-derived registry. It is reproducible from configuration and registered roots, but its IDs are physical parents, so ordinary scans update rows in place.
- Rebuildability: `source-rebuildable` — recreate registry rows through approved scans, then rescan source files. Preserve the database for user-curated links and review state that may refer to descendant IDs.
- Deletion effect: physical parent of `source_files`, `sessions`, `usage_records`, and `context_documents`, all `ON DELETE CASCADE`. Deleting a source would remove normalized descendants and can invalidate application-enforced links or search rows, so no cleanup is implied.
- Runtime references inspected: `src/localbrain/activity.py`, `src/localbrain/cli.py`, `src/localbrain/db.py`, `src/localbrain/ingest/scanner.py`, `src/localbrain/main.py`, `src/localbrain/queries.py`, `src/localbrain/retrieval.py`, `src/localbrain/runner.py`, `src/localbrain/schema_presentation.py`, `src/localbrain/usage_queries.py`, `src/localbrain/workstreams.py`.
- Synthetic tests inspected: `tests/test_ingest_policy.py`, `tests/test_session_contract.py`, `tests/test_usage_dashboard.py`.

### Evidence: source_files

<a id="evidence-source-files"></a>

- Subject owner: `source-registry-and-scans`; current semantic owner: `docs/policies/project/data-model/source-registry-and-scans.md`.
- Fresh / compatible source: fresh definition in `schema.sql`; `db.py` compatibly adds `usage_contract_version` and uses it to trigger staleness. No explicit named index.
- Purpose and authority: per-source scan evidence for a local input path, including its file fingerprint, latest attempt state, error, and usage-normalizer contract version.
- Producers: `ingest/scanner.py` inserts and updates scan results; `contexts.py` removes Context-owned rows when a root is disabled; `db.py` adds `usage_contract_version` and marks Claude/Codex rows stale when Session or Usage contracts change.
- Consumers: `ingest/scanner.py` skip/retry logic, `queries.py` source status, `usage_queries.py` freshness/trust, and `contexts.py` root lifecycle.
- Lifecycle: Source-derived and rebuildable — source-derived and rebuildable. Healthy unchanged rows are freshness inputs; failed rows retain the latest error rather than masquerading as absent.
- Rebuildability: `source-rebuildable` — rescan the owning source. Errors and exact prior attempt times are operational history and are not reproduced exactly.
- Deletion effect: `source_id` physically cascades from `sources`. The correlation from `path` to `sessions.source_path` or `context_documents.path` is application-managed; removing a file row alone does not cascade those objects. Scanner reconciliation explicitly removes disappeared normalized rows and their search entries.
- Runtime references inspected: `src/localbrain/contexts.py`, `src/localbrain/db.py`, `src/localbrain/ingest/scanner.py`, `src/localbrain/queries.py`, `src/localbrain/usage_queries.py`.
- Synthetic tests inspected: `tests/test_session_contract.py`, `tests/test_usage_contract.py`.

### Evidence: workspaces

<a id="evidence-workspaces"></a>

- Subject owner: `workspace-and-session-activity`; current semantic owner: `docs/policies/project/data-model/workspace-and-session-activity.md`.
- Fresh / compatible source: fresh definition in `schema.sql`; compatible migration in `db.py` drops legacy `git_branch`. No explicit named index.
- Purpose and authority: current canonical project/workspace identity discovered from Session working directories; the containing Git repository root is current discovery, not historical branch state.
- Producers: `ingest/scanner.py` upserts path, display name, Git root, existence, and activity; `db.py` removes the obsolete `git_branch` column during compatible migration.
- Consumers: inventory/detail queries, retrieval, Workstream resource pickers and links, Runner context, activity attribution, and Usage Record attribution/browse joins.
- Lifecycle: Mixed source identity and curated references — mixed source identity and curated references. Discovery can rebuild path metadata, but Workstream/Thread links and Usage Record snapshot browsing use its stable ID, so delete-and-recreate is not equivalent to update-in-place.
- Rebuildability: `conditional-stable-id` — rescan Sessions to rediscover paths. Preserve or restore the database to retain stable link targets and historical current-row resolution.
- Deletion effect: physical optional parent of `sessions` and `context_documents`, both `SET NULL`. It is an application target for `project` polymorphic links and `usage_records.workspace_id_snapshot`; those are not cascaded by SQLite.
- Runtime references inspected: `src/localbrain/db.py`, `src/localbrain/ingest/scanner.py`, `src/localbrain/queries.py`, `src/localbrain/retrieval.py`, `src/localbrain/runner.py`, `src/localbrain/usage.py`, `src/localbrain/usage_queries.py`, `src/localbrain/workstreams.py`.
- Synthetic tests inspected: `tests/test_session_inventory.py`, `tests/test_workstreams.py`, `tests/test_usage_dashboard.py`.

### Evidence: sessions

<a id="evidence-sessions"></a>

- Subject owner: `workspace-and-session-activity`; current semantic owner: `docs/policies/project/data-model/workspace-and-session-activity.md`.
- Fresh / compatible source: fresh definition and `idx_sessions_last_event`, `idx_sessions_workspace` in `schema.sql`; compatible columns, backup-backed classification/Run-link table repair, and runtime-only `idx_sessions_class`, `idx_sessions_role`, `idx_sessions_parent` in `db.py`. Before that structural repair, startup preserves `localbrain.db-pre-maintenance-session-contract-v1.bak` and validates it with SQLite `quick_check`. An older additive layout may have the same approved columns in a different physical order; migration validates every name/type/null/default/key contract, copies values by canonical column name, and converges only the physical order.
- Purpose and authority: one normalized Claude or Codex Session/subsession per native source external identity. An in-app Task Runner Run is represented by its persisted native Claude primary Session, linked to the Run ledger rather than duplicated by LocalBrain.
- Producers: `ingest/scanner.py` plus Claude/Codex parsers create Sessions and Usage Records; parent reconciliation updates self-references and propagates maintenance policy to Claude children. `runner.py` triggers Claude-only synchronization after terminal and recovered Runs. `db.py` owns the classification/Run-link constraint repair and stales source files when parser contracts change.
- Consumers: Session inventory/detail, dashboard counts, normalized activity, retrieval, Workstream linking/suggestions, Runner context, search projection, Usage Records, and Usage Dashboard denominators.
- Lifecycle: Source-derived and rebuildable — all Sessions are source-derived from retained native JSONL. Stable IDs are preserved so curated links and Usage Records remain valid. A Run-linked primary and its resolved child Sessions keep metadata while maintenance index/event policy is restricted; the private Runner stream is an operational artifact only.
- Rebuildability: `source-rebuildable` — rescan the authoritative native source file and reconcile parents. Restoring only a Task Runner stream cannot rebuild a Session or Usage Record; restore native Claude JSONL and the Run ledger together. A full database rebuild cannot restore user-curated links, exact operational history, or confirmed review state without backup.
- Deletion effect: physical `source_id` cascades; optional `workspace_id`, `parent_session_id`, and unique `maintenance_run_id` set null. Deleting a Session cascades `activity_events` and `usage_records`; scanner also removes its search row. Polymorphic links and checkpoint refs are application edges and can retain an unresolved historical ID.
- Runtime references inspected: `src/localbrain/activity.py`, `src/localbrain/config.py`, `src/localbrain/db.py`, `src/localbrain/ingest/scanner.py`, `src/localbrain/main.py`, `src/localbrain/queries.py`, `src/localbrain/retrieval.py`, `src/localbrain/runner.py`, `src/localbrain/usage_queries.py`, `src/localbrain/workstreams.py`.
- Synthetic tests inspected: `tests/test_session_contract.py`, `tests/test_session_details.py`, `tests/test_session_inventory.py`, `tests/test_usage_contract.py`.

### Evidence: activity_events

<a id="evidence-activity-events"></a>

- Subject owner: `workspace-and-session-activity`; current semantic owner: `docs/policies/project/data-model/workspace-and-session-activity.md`.
- Fresh / compatible source: fresh definition in `schema.sql`; event ordering uses the leading `session_id, sequence` keys of the table's UNIQUE autoindex. Compatible migration removes the former redundant named index only after verifying that coverage and removes the legacy metadata column only when every value is `NULL`.
- Purpose and authority: normalized, ordered Session events used for conversation presentation, activity calculation, retrieval evidence, and tool/message counts without exposing raw event payload files.
- Producers: `ingest/scanner.py` from normalized parser events.
- Consumers: `queries.py` Session detail and counts, `activity.py` active-time attribution, and `retrieval.py` evidence loading.
- Lifecycle: Source-derived and rebuildable — source-derived and rebuildable. Scanner replaces the Session's event set atomically during re-import.
- Rebuildability: `source-rebuildable` — rescan the Session source. Opaque tool arguments/results are not indexed by default; a later concrete metadata requirement must introduce an explicit bounded contract rather than relying on an unused generic JSON slot.
- Deletion effect: `session_id` cascades from `sessions`; replacement deletes prior rows before inserting the new normalized sequence.
- Runtime references inspected: `src/localbrain/activity.py`, `src/localbrain/db.py`, `src/localbrain/ingest/scanner.py`, `src/localbrain/queries.py`, `src/localbrain/retrieval.py`.
- Synthetic tests inspected: `tests/test_activity_attribution.py`, `tests/test_session_details.py`, `tests/test_retrieval.py`, `tests/test_schema_migrations.py`.

### Evidence: usage_price_snapshots

<a id="evidence-usage-price-snapshots"></a>

- Subject owner: `usage-and-cost-records`; current semantic owner: `docs/policies/project/data-model/usage-and-cost-records.md`.
- Fresh / compatible source: fresh definition in `schema.sql`; seed rows are owned by `usage.py`; no explicit named index.
- Purpose and authority: immutable identity and provenance for a versioned local pricing set used by estimated-cost calculations.
- Producers: `usage.py` startup seeding.
- Consumers: `usage.py` calculation/version validation and `usage_queries.py` trust/provenance presentation.
- Lifecycle: Code-seeded reference — code-seeded reference. `usage.ensure_default_price_snapshot` inserts known versions without overwriting existing evidence.
- Rebuildability: `code-rebuildable` — reproduce the exact application version's seeded snapshots. Database backup is preferred when retaining records calculated by an older version.
- Deletion effect: physical parent of model prices and Usage Records with `ON DELETE RESTRICT`; cited pricing evidence cannot be removed.
- Runtime references inspected: `src/localbrain/usage.py`, `src/localbrain/usage_queries.py`.
- Synthetic tests inspected: `tests/test_usage_contract.py`.

### Evidence: usage_model_prices

<a id="evidence-usage-model-prices"></a>

- Subject owner: `usage-and-cost-records`; current semantic owner: `docs/policies/project/data-model/usage-and-cost-records.md`.
- Fresh / compatible source: fresh definition in `schema.sql`; `db.py` compatibly adds all five long-context fields.
- Purpose and authority: per-model decimal price strings for one immutable snapshot, including optional long-context thresholds and tiered rates.
- Producers: `usage.py` idempotent snapshot seeding.
- Consumers: `usage.py` price selection and exact Decimal-based calculation.
- Lifecycle: Code-seeded reference — code-seeded reference and physically restricted once its snapshot is cited.
- Rebuildability: `code-rebuildable` — recreate from the same versioned code constants. Changing a rate requires a new snapshot, not mutation of cited rows.
- Deletion effect: composite identity belongs to `usage_price_snapshots`; deleting the parent is restricted.
- Runtime references inspected: `src/localbrain/db.py`, `src/localbrain/usage.py`.
- Synthetic tests inspected: `tests/test_usage_contract.py`.

### Evidence: usage_records

<a id="evidence-usage-records"></a>

- Subject owner: `usage-and-cost-records`; current semantic owner: `docs/policies/project/data-model/usage-and-cost-records.md`.
- Fresh / compatible source: fresh `usage_records` definition and three time indexes in `schema.sql`; `db.py` performs a one-way in-place rename from legacy `usage_facts`, replaces only the explicit legacy index names, and owns attribution and normalizer compatible columns. It refuses startup if legacy and canonical tables coexist. Older compatible tables retain their historical column metadata, including nullable `attributed_at`, while a backup-backed transactional repair adds only the missing three-value `attribution_basis` CHECK after exact full-row preflight and comparison.
- Purpose and authority: one normalized direct or aggregate usage observation per source record, with token semantics, capability/calculation state, cited pricing, and frozen Project attribution.
- Producers: Claude/Codex native source normalizers feed `usage.py`; source repair replaces a source's complete record set, including Run-linked Maintenance Sessions. The Runner stream is not a Usage producer. `db.py` compatibly adds attribution, normalizer, and snapshot fields and backfills missing basis/time.
- Consumers: `usage.py` repair/idempotency, `usage_queries.py` period summary/history/breakdowns/trust, and activity/project attribution tests. Dashboard selection excludes exact Claude `<synthetic>` pseudo-model rows without deleting them.
- Lifecycle: Mixed source identity and curated references — mixed source-derived and immutable historical evidence. Source observations can be re-normalized, but exact calculated cost and Project attribution must retain their cited versions and snapshot values.
- Rebuildability: `conditional-stable-id` — native source records plus the correct normalizer/calculator and price snapshots can rebuild observations, but a Runner stream alone cannot and current paths may not reproduce historical Project attribution. Before repairing a legacy attribution CHECK, startup preserves `localbrain.db-pre-usage-attribution-check-v1.bak` beside the database and validates it with SQLite `quick_check`.
- Deletion effect: physical source and Session parents cascade; pricing snapshot deletion is restricted. `workspace_id_snapshot` is an application-only optional current browse join, never attribution authority.
- Runtime references inspected: `src/localbrain/db.py`, `src/localbrain/ingest/claude.py`, `src/localbrain/ingest/codex.py`, `src/localbrain/ingest/common.py`, `src/localbrain/ingest/scanner.py`, `src/localbrain/usage.py`, `src/localbrain/usage_queries.py`.
- Synthetic tests inspected: `tests/test_usage_contract.py`, `tests/test_usage_dashboard.py`, `tests/test_activity_attribution.py`, `tests/test_schema_migrations.py`.

### Evidence: context_roots

<a id="evidence-context-roots"></a>

- Subject owner: `local-context-corpus`; current semantic owner: `docs/policies/project/data-model/local-context-corpus.md`.
- Fresh / compatible source: fresh definition in `schema.sql`; `db.py` compatibly adds `source_type`, `readable`, `status`, and `error` and migrates document ownership.
- Purpose and authority: user-managed registry of folders, individual files, and Apple Notes sources, including readability, enablement, scan status, and error state.
- Producers: `contexts.py` adds, restores, disables, and validates roots; `ingest/scanner.py` updates scan outcomes; `db.py` compatibly seeds/migrates older folder roots.
- Consumers: Context source listing/detail in `contexts.py` and `queries.py`, scanner source enumeration, Workstream pickers/suggestions, and document visibility filters.
- Lifecycle: User-curated and non-rebuildable — user-curated and non-rebuildable as a registry. A default Context directory may be discovered compatibly, but user additions/removals and source types require backup.
- Rebuildability: `non-rebuildable` — restore the LocalBrain database or re-register every root manually, then scan. Original files/Notes remain external authorities.
- Deletion effect: optional physical parent of `context_documents` with `ON DELETE SET NULL`; ordinary removal disables the root and removes indexed rows for that source boundary rather than deleting the registry row or original local content.
- Runtime references inspected: `src/localbrain/contexts.py`, `src/localbrain/db.py`, `src/localbrain/ingest/scanner.py`, `src/localbrain/queries.py`, `src/localbrain/workstreams.py`.
- Synthetic tests inspected: `tests/test_context_roots.py`, `tests/test_session_contract.py`.

### Evidence: context_documents

<a id="evidence-context-documents"></a>

- Subject owner: `local-context-corpus`; current semantic owner: `docs/policies/project/data-model/local-context-corpus.md`.
- Fresh / compatible source: fresh definition plus `idx_documents_mtime` in `schema.sql`; compatible `context_root_id` and `content_type`, migration assignment, and runtime-only `idx_documents_context_root` in `db.py`.
- Purpose and authority: normalized text and metadata indexed from a registered local file/folder/Notes source, with source path, content fingerprint, and optional current workspace association.
- Producers: `ingest/scanner.py` upserts documents and removes disappeared rows; `db.py` migrates older root associations and relative paths.
- Consumers: Context browse/detail in `queries.py` and `main.py`, retrieval evidence, Runner context manifests, Workstream links/suggestions, source summaries, and `search_index` projection.
- Lifecycle: Source-derived and rebuildable — source-derived and rebuildable while the external source remains available. The stored body is local indexed content and is never copied into tracked schema evidence.
- Rebuildability: `source-rebuildable` — rescan external sources. If the original content is gone, only a database/host backup can recover the indexed body; LocalBrain is not the authority for the original file.
- Deletion effect: source deletion cascades. Context root and workspace deletion set their keys null, retaining the document until scanner/root lifecycle removes it. Scanner and `contexts.py` explicitly remove matching FTS rows. It is an application target for Workstream, Thread, and checkpoint references.
- Runtime references inspected: `src/localbrain/contexts.py`, `src/localbrain/db.py`, `src/localbrain/ingest/scanner.py`, `src/localbrain/main.py`, `src/localbrain/queries.py`, `src/localbrain/retrieval.py`, `src/localbrain/runner.py`, `src/localbrain/workstreams.py`.
- Synthetic tests inspected: `tests/test_context_roots.py`, `tests/test_retrieval.py`, `tests/test_schema_migrations.py`.

### Evidence: workstreams

<a id="evidence-workstreams"></a>

- Subject owner: `work-organization-and-resources`; current semantic owner: `docs/policies/project/data-model/work-organization-and-resources.md`.
- Fresh / compatible source: fresh definition in `schema.sql`; no compatible columns or explicit named index.
- Purpose and authority: user-created top-level organization with current status, summary, and activity marker.
- Producers: `workstreams.py` create/update/touch operations.
- Consumers: Dashboard and Workstream detail, retrieval scope, Runner context and Run association, Suggestions, Threads, checkpoints, and resource memberships.
- Lifecycle: User-curated and non-rebuildable — user-curated and non-rebuildable.
- Rebuildability: `non-rebuildable` — LocalBrain database backup; source rescans cannot recover names, summaries, statuses, or grouping intent.
- Deletion effect: physical parent of `threads`, `workstream_links`, and `checkpoints` with cascade; optional parent of `maintenance_runs` with set null. No ordinary delete workflow currently treats this as disposable.
- Runtime references inspected: `src/localbrain/contexts.py`, `src/localbrain/db.py`, `src/localbrain/main.py`, `src/localbrain/runner.py`, `src/localbrain/workstreams.py`.
- Synthetic tests inspected: `tests/test_workstreams.py`, `tests/test_runner.py`.

### Evidence: threads

<a id="evidence-threads"></a>

- Subject owner: `work-organization-and-resources`; current semantic owner: `docs/policies/project/data-model/work-organization-and-resources.md`.
- Fresh / compatible source: fresh definition and `idx_threads_workstream` in `schema.sql`; `db.py` repeats the index idempotently.
- Purpose and authority: ordered, nested work track inside one Workstream, including status, summary, goal, and next action.
- Producers: `workstreams.py` create and update operations.
- Consumers: Workstream detail, retrieval scoping, Runner structured results, Suggestions, links, checkpoints, and membership queries.
- Lifecycle: User-curated and non-rebuildable — user-curated and non-rebuildable.
- Rebuildability: `non-rebuildable` — database backup. Recreating a title does not recreate identity, order, links, or checkpoint history.
- Deletion effect: Workstream deletion cascades; Thread deletion cascades `thread_links` and sets historical `checkpoint_resource_refs.thread_id` null.
- Runtime references inspected: `src/localbrain/db.py`, `src/localbrain/main.py`, `src/localbrain/retrieval.py`, `src/localbrain/runner.py`, `src/localbrain/workstreams.py`.
- Synthetic tests inspected: `tests/test_workstreams.py`, `tests/test_runner.py`, `tests/test_retrieval.py`.

### Evidence: workstream_links

<a id="evidence-workstream-links"></a>

- Subject owner: `work-organization-and-resources`; current semantic owner: `docs/policies/project/data-model/work-organization-and-resources.md`.
- Fresh / compatible source: fresh definition and `idx_workstream_links_entity` in `schema.sql`; relation type compatible addition and repeated index in `db.py`.
- Purpose and authority: one reviewed or generated relationship from a Workstream to a heterogeneous resource, shared by multiple Threads.
- Producers: `workstreams.py` validated add/update/remove operations and accepted suggestions.
- Consumers: Workstream detail, latest activity, memberships, retrieval exclusions/scope, Runner manifests, checkpoint snapshots, and suggestion deduplication.
- Lifecycle: User-curated and non-rebuildable — user-curated and non-rebuildable; `linked_by` and `confidence` retain origin context.
- Rebuildability: `non-rebuildable` — database backup or accepted external workflow record; source scanning does not infer the same relation reliably.
- Deletion effect: Workstream deletion cascades. Target edges are application-enforced and do not cascade; target removal can leave historical/unresolved identity.
- Runtime references inspected: `src/localbrain/db.py`, `src/localbrain/retrieval.py`, `src/localbrain/workstreams.py`.
- Synthetic tests inspected: `tests/test_workstreams.py`, `tests/test_retrieval.py`.

### Evidence: thread_links

<a id="evidence-thread-links"></a>

- Subject owner: `work-organization-and-resources`; current semantic owner: `docs/policies/project/data-model/work-organization-and-resources.md`.
- Fresh / compatible source: fresh definition and `idx_thread_links_entity` in `schema.sql`; `db.py` repeats the index idempotently.
- Purpose and authority: one reviewed or generated relationship from a Thread to a heterogeneous resource.
- Producers: `workstreams.py` validated add/update/remove operations and accepted suggestions.
- Consumers: Workstream/Thread detail, activity, memberships, retrieval scope, Runner manifests, checkpoint snapshots, and suggestion deduplication.
- Lifecycle: User-curated and non-rebuildable — user-curated and non-rebuildable.
- Rebuildability: `non-rebuildable` — database backup; inference may suggest a new link but cannot restore review provenance exactly.
- Deletion effect: Thread deletion cascades. Target relations are application-enforced and have no SQLite cascade.
- Runtime references inspected: `src/localbrain/db.py`, `src/localbrain/retrieval.py`, `src/localbrain/workstreams.py`.
- Synthetic tests inspected: `tests/test_workstreams.py`, `tests/test_runner.py`, `tests/test_retrieval.py`.

### Evidence: local_resources

<a id="evidence-local-resources"></a>

- Subject owner: `work-organization-and-resources`; current semantic owner: `docs/policies/project/data-model/work-organization-and-resources.md`.
- Fresh / compatible source: fresh definition in `schema.sql`; path lookup and uniqueness use the `UNIQUE(path)` autoindex, and the compatible migration removes the former redundant named path index only after verifying that coverage.
- Purpose and authority: canonical user-visible local repository, directory, file, or generic path resource independent of indexed Context Documents.
- Producers: `workstreams.py` canonical registration and update; Runner structured output can propose/register reviewed local resources.
- Consumers: resource picker/detail, Workstream/Thread link resolution, retrieval context, and Runner reference manifests.
- Lifecycle: User-curated and non-rebuildable — user-curated and non-rebuildable as organization, even though existence can be rechecked.
- Rebuildability: `non-rebuildable` — database backup plus local filesystem. A path scan cannot reconstruct title, summary, discovery provenance, or memberships.
- Deletion effect: application target of link and checkpoint tables only; SQLite does not cascade target deletion.
- Runtime references inspected: `src/localbrain/db.py`, `src/localbrain/retrieval.py`, `src/localbrain/runner.py`, `src/localbrain/workstreams.py`.
- Synthetic tests inspected: `tests/test_runner.py`, `tests/test_workstreams.py`, `tests/test_schema_migrations.py`.

### Evidence: external_resources

<a id="evidence-external-resources"></a>

- Subject owner: `work-organization-and-resources`; current semantic owner: `docs/policies/project/data-model/work-organization-and-resources.md`.
- Fresh / compatible source: fresh definition in `schema.sql`; no compatible migration or explicit named index.
- Purpose and authority: canonical user-visible external reference such as Jira, Wiki, Slack, Git, document, or generic URL; storage does not imply external ingestion or writes.
- Producers: `workstreams.py` registration/update and reviewed Runner structured output.
- Consumers: resource picker/detail, link resolution, retrieval context, and Runner manifests.
- Lifecycle: User-curated and non-rebuildable — user-curated and non-rebuildable.
- Rebuildability: `non-rebuildable` — database backup or manual re-entry from the original reference system.
- Deletion effect: application target of link and checkpoint tables only; no SQLite cascade. Approved integrations remain read-only unless separately planned.
- Runtime references inspected: `src/localbrain/retrieval.py`, `src/localbrain/runner.py`, `src/localbrain/workstreams.py`.
- Synthetic tests inspected: `tests/test_runner.py`, `tests/test_workstreams.py`.

### Evidence: suggestions

<a id="evidence-suggestions"></a>

- Subject owner: `review-and-resume-continuity`; current semantic owner: `docs/policies/project/data-model/review-and-resume-continuity.md`.
- Fresh / compatible source: fresh definition and `idx_suggestions_target` in `schema.sql`; compatible `origin_run_id`, repeated target index, and runtime-only `idx_suggestions_origin_run` in `db.py`.
- Purpose and authority: proposed organizational or checkpoint changes with evidence, deterministic fingerprint, provenance, confidence, and explicit pending/accepted/rejected review state.
- Producers: quick-match generation and refresh in `workstreams.py`; structured Task Runner output and reconciliation in `runner.py`.
- Consumers: Dashboard and Workstream review queues, retrieval exclusions/context, accept/reject actions, and pending counts.
- Lifecycle: Generated review state — generated review state. Pending content can be regenerated, but accepted/rejected decisions, resolution times, and original Run provenance are non-rebuildable history.
- Rebuildability: `partial-review-history` — rerun generation for new pending proposals; restore the database for historical review decisions and exact provenance.
- Deletion effect: `target_type` plus `target_id` application-addresses a Workstream or Thread; `origin_run_id` application-addresses `maintenance_runs.id`. No physical cascade applies. Refresh replaces eligible pending suggestions while preserving rejected decisions and other protected state.
- Runtime references inspected: `src/localbrain/db.py`, `src/localbrain/main.py`, `src/localbrain/retrieval.py`, `src/localbrain/runner.py`, `src/localbrain/workstreams.py`.
- Synthetic tests inspected: `tests/test_runner.py`, `tests/test_workstreams.py`.

### Evidence: checkpoints

<a id="evidence-checkpoints"></a>

- Subject owner: `review-and-resume-continuity`; current semantic owner: `docs/policies/project/data-model/review-and-resume-continuity.md`.
- Fresh / compatible source: fresh definition in `schema.sql`; compatible `based_on_activity_at`, `version`, and `updated_at` plus value backfill in `db.py`. On an older database, `updated_at` remains nullable and has no database default after the one-time backfill. No explicit named index.
- Purpose and authority: versioned, human-confirmed resume state for a Workstream, including facts, decisions, open questions, next actions, files, and the activity boundary it summarizes.
- Producers: `workstreams.py` creates the next Workstream-scoped version and snapshots current links.
- Consumers: Workstream detail/resume presentation and checkpoint history.
- Lifecycle: Confirmed continuity snapshot — confirmed continuity snapshot; user-curated and non-rebuildable.
- Rebuildability: `non-rebuildable-snapshot` — database backup or an explicit export; Sessions and documents do not reconstruct confirmed wording or decision boundaries.
- Deletion effect: Workstream deletion cascades checkpoints, then checkpoint resource references. This destructive path is not an implied cleanup action.
- Runtime references inspected: `src/localbrain/db.py`, `src/localbrain/main.py`, `src/localbrain/workstreams.py`.
- Synthetic tests inspected: `tests/test_workstreams.py`, `tests/test_schema_migrations.py`.

### Evidence: checkpoint_resource_refs

<a id="evidence-checkpoint-resource-refs"></a>

- Subject owner: `review-and-resume-continuity`; current semantic owner: `docs/policies/project/data-model/review-and-resume-continuity.md`.
- Fresh / compatible source: fresh definition in `schema.sql`; checkpoint/thread lookup uses the leading keys of the table's UNIQUE autoindex, and the compatible migration removes the former redundant named index only after verifying that coverage.
- Purpose and authority: immutable-at-creation snapshot of Workstream- and Thread-level links included in one checkpoint.
- Producers: `_snapshot_checkpoint_resources` in `workstreams.py` copies current Workstream/Thread links with `INSERT OR IGNORE`.
- Consumers: checkpoint detail and resume evidence in `workstreams.py`.
- Lifecycle: Confirmed continuity snapshot — confirmed continuity snapshot. It is generated from curated links at checkpoint time but cannot be regenerated after those links change.
- Rebuildability: `non-rebuildable-snapshot` — restore the database. Re-running a snapshot produces current, not historical, membership.
- Deletion effect: checkpoint deletion cascades; Thread deletion sets `thread_id` null while retaining the captured heterogeneous identity. `entity_type` and `entity_id` target Session, Context Document, Workspace, External Resource, or Local Resource by application contract only.
- Runtime references inspected: `src/localbrain/db.py`, `src/localbrain/workstreams.py`.
- Synthetic tests inspected: `tests/test_workstreams.py`, `tests/test_schema_migrations.py`.

### Evidence: maintenance_runs

<a id="evidence-maintenance-runs"></a>

- Subject owner: `maintenance-execution`; current semantic owner: `docs/policies/project/data-model/maintenance-execution.md`.
- Fresh / compatible source: fresh definition in `schema.sql`; numerous compatible fields, `updated_at` value backfill, runtime-only `idx_maintenance_runs_workstream` and `idx_maintenance_runs_status`, and the backup-backed Workstream FK repair in `db.py`. Before the first repair, startup preserves `localbrain.db-pre-maintenance-workstream-fk-v1.bak`, rejects orphan or unexpected shapes, copies every Run value by column name, and verifies dependent Session/Suggestion links plus `foreign_key_check`. Compatible `updated_at` remains nullable without a database default after backfill.
- Purpose and authority: durable state machine for prepared/running/terminal maintenance execution, with local process, bounded MCP budget, artifact paths, structured output, summary, and error evidence.
- Producers: `runner.py` prepares, starts, streams, reconciles, cancels, parses, and completes Runs. Task Runner preparation writes private artifacts and inserts a `queued` ledger row; terminal and recovered paths trigger native Claude-source synchronization. `POST /api/workstreams/{workstream_id}/runs` is the only in-app Run creation boundary.
- Consumers: Runner list/detail/polling, Workstream detail, restart reconciliation, structured Suggestion creation, the linked Maintenance Session, and its Usage Records.
- Lifecycle: Operational history — operational history. Database state is durable, while referenced manifest/prompt/stream/result/stderr files live under the private runtime data directory and are required for complete evidence recovery.
- Rebuildability: `database-plus-artifacts` — restore the SQLite database, native Claude source history, and runtime artifact directory according to the evidence required. The native JSONL can rebuild its Session and Usage Records; a retained Runner stream can recover console/result evidence but cannot create them. A failed Run must not clear an existing review queue.
- Deletion effect: `workstream_id` is an optional physical FK to `workstreams.id` with `ON DELETE SET NULL` in both fresh and upgraded databases. Deleting the Workstream removes only that current association and retains the Run. `sessions.maintenance_run_id` is a nullable unique physical FK with `ON DELETE SET NULL`; `suggestions.origin_run_id` remains an application edge without cascade.
- Runtime references inspected: `src/localbrain/db.py`, `src/localbrain/ingest/scanner.py`, `src/localbrain/main.py`, `src/localbrain/runner.py`.
- Synthetic tests inspected: `tests/test_runner.py`, `tests/test_schema_migrations.py`.

### Evidence: search_index

<a id="evidence-search-index"></a>

- Subject owner: `derived-retrieval-index`; current semantic owner: `docs/policies/project/data-model/derived-retrieval-index.md`.
- Fresh / compatible source: FTS5 virtual-table declaration in `schema.sql`; no compatible migration or separate explicit named index. FTS5 shadow tables are SQLite internals.
- Purpose and authority: FTS5 search projection for eligible primary work Sessions and enabled Context Documents, retaining unindexed target/provenance fields beside indexed title/body/path text.
- Producers: `ingest/scanner.py` replaces Session and document rows during scan; `contexts.py` deletes root-owned document projections on disable; source cleanup deletes projections before normalized objects.
- Consumers: `queries.py` search results and `retrieval.py` ranked FTS candidates. Retrieval adds current relational evidence after candidate selection.
- Lifecycle: Fully derived projection — fully derived projection. It is safe to clear and rebuild from current eligible source objects.
- Rebuildability: `fully-derived` — rescan all enabled Session and Context sources. No backup is required for this table itself, though authoritative sources and user registration must exist.
- Deletion effect: no physical FK. `entity_type` plus `entity_id` application-addresses `sessions` or `context_documents`; scanner/context lifecycle explicitly removes projections. Maintenance Sessions, subsessions, and policy-excluded content do not enter the projection.
- Runtime references inspected: `src/localbrain/contexts.py`, `src/localbrain/ingest/scanner.py`, `src/localbrain/queries.py`, `src/localbrain/retrieval.py`.
- Synthetic tests inspected: `tests/test_retrieval.py`, `tests/test_context_roots.py`, `tests/test_ingest_policy.py`.
