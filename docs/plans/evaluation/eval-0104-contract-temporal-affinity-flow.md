# EVAL-0104 Contract: Temporal Affinity Flow

- Status: `complete`
- Result: `PASS`
- Evidence Coverage: `complete`
- Feature: [FEAT-0104](../feature/feat-0104-temporal-affinity-flow.md)
- Spec: [SPEC-0104](../spec/spec-0104-temporal-affinity-flow.md)
- Run: [RUN-117](../run/run-20260924-117-continuous-affinity-canvas.md), attempt `1`
- Profile: `fullstack-product`; lane: `backend`

## Current Continuous-Canvas Result

25 reader tests pass. A 65-group/three-page fixture proves full `map_nodes`,
page-independent `map_scope`/geometry/edges, cross-page connections and exact
off-page period evidence. Search/hierarchy own graph scope; inventory is
text-only. The three observational levels use the same contract. Existing
freshness, owner, locking, read-only bytes and exact source checks still pass.
The previous reader shape renders the native list safely until process restart.

The `v2` projection changes presentation, not report format, source authority,
snapshot identity or persistence. SPEC-0101 and architecture now distinguish
text pages from the whole-scope map. Producer, template, JS, fixtures and browser
checks agree; no 24-node guard or list-page camera identity remains. No model
execution, new database table or private-data mutation.

## Historical RUN-116 Evidence

The earlier checks below did not establish continuous map navigation. The
owner-returned paging assumption is superseded by the current result above.

22 reader tests pass, including four added temporal cases: one common domain
before search/paging, bounded day-multiple bins, occurrence conservation,
exact mean-time anchors, months-long gaps, unknown-time accounting, valid period
evidence and rejected invalid periods, deterministic original edge witnesses.
Existing all-history, source revalidation, exact quote/locator, owner/expiry,
lock, cache, stale/missing/invalid and read-only checks still pass.

This is an additive presentation of the existing qualified report, not a new
report schema, persistence owner, model job or identity contract. GET cannot
prepare data. Group/evidence paging and period selection do not collapse duplicate
occurrences. Unknown-time witnesses do not acquire artificial dated endpoints.
Production work identity and causal continuity are unverified and out of scope,
not acceptance gaps for this observation surface.
