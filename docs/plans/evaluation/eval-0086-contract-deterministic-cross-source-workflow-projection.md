# EVAL-0086 Contract: Deterministic Cross-Source Workflow Projection

## Metadata

- ID: `eval-0086-contract`
- Status: `complete`
- Evaluator Type: `contract`
- Result: `PASS`
- Run: [RUN-20260914-96](../run/run-20260914-96-deterministic-cross-source-workflow-projection.md)
- Attempt: `1`
- Feature: [FEAT-0086](../feature/feat-0086-deterministic-cross-source-workflow-projection.md)
- Spec: [SPEC-0086](../spec/spec-0086-deterministic-cross-source-workflow-projection.md)
- Execution Profile: `foundation-contract`
- Surface Lane: `Cross-source Focus read model and durable owners`
- Evidence Coverage: `complete`
- Created: `2026-09-14`

## Checks And Evidence

- `workflow_focus.py` is a versioned, route-independent SQLite read model. It
  returns typed `ready`, `missing`, and `ineligible` results; `ready` always
  contains the selected eligible primary work Session as one FEAT-0085 Episode.
- Candidate admission uses exact user Thread or approved conjunctive reference,
  Workstream, workspace, and Git signals. Generated membership and isolated
  workspace/root/branch/time similarity cannot create direction. At most 200
  qualified candidates enter classification.
- The producer emits adjacent `continues` and nearest-earlier
  `branches-from`, retains one derived incoming relation per Episode, delegates
  validation to FEAT-0085, and never infers `merged-into` from branch names.
- Stable graph traversal enforces 24 Episodes, four outward branch roots, and
  four Episodes within one traversed branch. Evidence grouping enforces five
  expanded items per family and carries retained/observed totals plus partial
  and stale state.
- Current Related Materials supplies safe direct and organized destinations;
  user organization membership, direct Subsessions, and persisted Atlassian
  structure-reference identity are additive evidence. Documents, Jira/Wiki,
  Resources, and Subsessions never enter the peer Episode collection.
- Serialized results omit native Session IDs, source paths, repository paths,
  bodies, excerpts, search text, and opaque payloads. Git reason identities are
  hashed. A SQL trace and `total_changes` fixture prove zero DML; no route,
  cache, schema, migration, source operation, remote call, or model dependency
  was introduced.
- Product, Architecture, Privacy, Workspace/Session Activity, and Work
  Organization owners agree. Data-model checks, generated Schema Presentation,
  and the 623-object cleanup audit are current.
- Focused verification passed `14/14`; the complete repository suite passed
  `512/512`. Privacy passed over `860` candidate files and `git diff --check`
  passed.

## Findings

- None.

## Current Route

- Current route: `PASS`; FEAT-0087 may render this payload without additional
  domain inference.
