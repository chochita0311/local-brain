# EVAL-0095 Contract: Bounded Work Reconstruction Experiment

## Metadata

- ID: `eval-0095-contract`
- Status: `complete`
- Evaluator Type: `contract`
- Result: `PASS`
- Run: [RUN-20260915-101](../run/run-20260915-101-bounded-work-reconstruction-experiment.md)
- Attempt: `2`
- Feature: [FEAT-0095](../feature/feat-0095-bounded-work-reconstruction-experiment.md)
- Spec: [SPEC-0095](../spec/spec-0095-bounded-work-reconstruction-experiment.md)
- Execution Profile: `foundation-contract`
- Surface Lanes: extraction/scoring → local experiment
- Evidence Coverage: `partial`
- Created: `2026-09-15`
- Updated: `2026-09-16`

## Scope And Environment

The five isolated `work_reconstruction*` modules, explicit script, synthetic
fixtures/tests, and changed Product/Architecture/Privacy owners. Evaluation was
performed by the primary agent in the existing macOS Python 3.9 environment.
Attempt 2 also used the owner-selected current database for an unassessed local
execution check, exposing only fixed status codes to the hosted agent. No
connector, model, or application server was started. This technical PASS is
not full FEAT-0095 acceptance.

## Contract Results

| Surface | Result and evidence |
| --- | --- |
| Input and authority | PASS: bounded, versioned snapshot and explicit manifest; exact hash/span/time binding; separate observed wording/locators and inferred fields/memberships; expected labels never enter extraction |
| Experimental identity | PASS: semantic goal identity separate from revisions and labels; exact correction targets bind record, offsets, and wording; equal-length edits deactivate old targets instead of retargeting them |
| Matched comparator | PASS: metadata receives spans and reference facts without bodies; all qualifying pairs pass through the unchanged FEAT-0090 normalizer; canonical Context/Site identities and global pair overflow are tested |
| Scoring | PASS: independent digest-bound expectations, integer denominators, explicit wrong/unassessed/fragmented counts, duplicate-assignment rejection, zero incompatible-outcome merges, and unknown-label handling |
| Read-only admission | PASS: real-schema synthetic SQLite tests enforce read-only/query-only/authorizer boundaries, pre-body eligibility, exact selected normalized text, source binding checks, no bootstrap, and unchanged database bytes |
| Runtime/output | PASS: supervised spawn-free workers, wall/memory failure and cancellation/reaping, fail-closed monitor loss, bounded JSON, new owner-only output outside Git, fixed default status output, and cleanup after publication failure |
| Production/source isolation | PASS: no route, schema, normalizer change, app consumer, correction-ledger write, source crawl, remote/model call, or legacy mutation; guarded pure-arm tests and existing regressions remain green |

## Producer And Consumer Evidence

- The input adapter consumes only explicitly selected existing indexed records.
  After owner scope selection, the preparer freezes an ordinal chronological
  sample independently of text meaning or predictions. Missing source
  permission/identity or invalid hashes fail closed; partial reference scans
  remain incomplete coverage. Both arms retain matched eligibility exclusions.
- The extractor recognizes a small explicit Korean/English action grammar and
  compatible exact subject/change/completion, not general semantic equivalence.
  Assistant outcome wording remains attributed and cannot close a flow.
- The scorer validates independent source spans and snapshot binding. A group
  cannot recover multiple incompatible outcomes or dilute errors with duplicate
  assignments. Missing/unknown assessment cannot become correction-free success.
- The command records per-arm results, resource measurements, chronological
  stability, and required review. `private_quality` is a numeric sample check;
  `viability` deliberately remains unverified until evaluator reconciliation.
  Exit 0 means a complete local report, not product acceptance.
- Current manual Workstream/Thread, Focus, assertions, and Related Materials
  remain runtime truth. The experiment has no production storage owner.

## Executed Verification

- Focused reconstruction tests: `61/61` passed.
- Full repository regression: `645/645` passed.
- Schema cleanup audit: current, `649` objects; `keep=543`, `change=0`,
  `remove=0`, `defer=106`. Refreshed only its generated runtime-use inventory
  for new SQL consumers; no schema or audit decision changed in this Run.
- Repository privacy scan and whitespace check passed. Final artifact-catalog
  and local-link checks are recorded with the Run closure evidence.

## Evidence Gaps And Findings

- No remaining implementation defect found within the tested technical scope.
- The current-data scope and bounded runtime execution are now verified.
  Independent expectations remain absent; explicit unknown placeholders cannot
  count as an answer key. No assessed local extraction-quality, weak-reference,
  chronological-quality, or correction-burden verdict exists. This is an
  assessment gap, not missing DB/period input or production-design authority.
- Authored linguistic cases are contract witnesses, not blinded held-out
  generalization evidence. They cannot establish whole-history extraction or
  Korean/English semantic adequacy outside the explicit grammar.
- Resource enforcement was exercised on macOS; Linux support is implemented but
  not executed here. No production latency, mixed-version server, or browser
  claim applies to this standalone non-UI experiment.

## Route

`pass` for the bounded technical surfaces; keep Feature and Run `blocked` with
partial evidence until independent local assessment is available. No additional
database-path, time-range, or unchanged scope approval is required.
Do not authorize production ownership, replacement UI, model work, or legacy
transition from these checks.

## Attempt 2 Evidence

- The owner named current LocalBrain data through now. Configuration resolution
  and read-only metadata preflight established a valid existing target; no body
  or identifying metadata entered hosted output.
- New tests cover chronological endpoints and sampling caps, unchanged replay,
  prediction independence, disabled/future/unsupported-time exclusions, source
  overflow before body reads, related Document/Item selection, owner-link
  attribution, retained omission coverage, and unassessed CLI behavior.
- Actual supervised preparation/comparison returned `EXPERIMENT_RECORDED`.
  The local wrapper verified the report was unassessed/insufficient/unverified,
  then removed all task-private artifacts. Only fixed verification/cleanup
  status codes crossed the tool boundary; no private quality measurements or
  corpus details are claimed in this report.
