# Workflow Map Design Plan

## Metadata

- Status: `active`
- Parent boundary:
  [PRD-0017](../prd/prd-0017-source-backed-workflow-map-and-workstream-lenses.md),
  `approved` for bounded reconstruction and the pre-quality inspection increment
- Created: `2026-09-14`
- Updated: `2026-09-24`
- Screen alignment: `reframe`
- Focus implementation alignment: `extend` within the current Session and
  Explorer families
- Current increment: continuous-canvas correction passed RUN-117 after owner review
  of RUN-116's arbitrary map paging. RUN-113–115
  delivered read readiness and a technically verified constellation, but the
  owner rejected its missing left-to-right time-flow structure.
  Passed data/infra foundations use `foundation-contract`; the Auto Work
  frontend/backend inspection extension uses `fullstack-product`, `extend` mode.
  Production ownership, replacement, correction and transition remain separate.

<a id="relation-first-inspection-track"></a>

## Direction Correction — 2026-09-24

This section records the delivered correction. The later owner-requested stop
and resume boundary are in [Handoff To Next Track](#handoff-to-next-track).

Current bounded batch: [RUN-117](../run/run-20260924-117-continuous-affinity-canvas.md)
separates the continuous map from the 24-item text list. This corrects scope and
interaction consistency, not grouping. Native time/row scrolling must retain
off-page affinity and stable geometry; list navigation must retain the camera.
Backend projection precedes native Explorer extension. Three-plus-page synthetic
data, cross-page edges, browser input/history/resize and private-safe readiness
are the exit gate. No new identity, storage, training or shell styling.

These gates now pass, including a 65-group three-page journey, larger 257-group
reachability, supported viewport renders and actual boolean-only readiness.
`design-plan` retained this existing owner; `screen-alignment` extended the
native Explorer with continuous scroll ownership and list-only pagination.

The owner clarified that the intended map is similarity-based, Gantt-like in
its left-to-right time orientation, but fluid and observational rather than a
static bar chart. A freeform constellation with dates only in its inspector
does not satisfy that direction. RUN-115 remains evidence of its narrower
technical checks, not acceptance of the requested visual result.

Under the then-active continuous-through-UI redesign authorization,
[FEAT-0104](../feature/feat-0104-temporal-affinity-flow.md) delivered the temporal
presentation: actual message time
owns x; similarity owns grouping and bounded vertical attraction. Observed
activity knots and curved strands replace spatially arbitrary circles. Gaps
are unobserved intervals, not continuous work or a closed/reopened lifecycle.
Time-anchored cross-strand links remain undirected similarity, not causal merges.

Screen-alignment first reframes the constellation-specific rule in constitution
v22, then extends the native Explorer/Trace family. Preserve the shell, tokens,
exact-source authority, all-history scope, paginated complete fallback, snapshot
selection, optional inspection and no model work on browse. Backend derives a
bounded temporal presentation from the already passed report; frontend follows
that contract. No new identity or persistence foundation is needed.

Exit gates: same date maps to the same x, months-long gaps remain visible,
unknown dates never acquire invented time, period selection reaches its exact
evidence, viewport/history stay coherent, and actual rendered curves, zoom,
focus lens, keyboard, reduced motion and fallback pass at supported widths.
The earlier constellation geometry below is historical and superseded for this
surface; the independent relation trial and production identity remain deferred.

RUN-116 passed its then-scoped evaluations with actual loopback readiness and
synthetic rendered interaction evidence. The local map uses the existing
calculation; no new model, identity or persistence work ran. Human observation
then exposed the pagination gap corrected by RUN-117 above. That historical
PASS does not establish the continuous-canvas contract on its own.

## Current Track: Whole-History Inspection And Independent Relations

The owner requested this planning review after the failed source-claim adapter.
The objective is still a low-maintenance map of enduring work areas and evolving
efforts, not a permanent generic similarity browser. The delivered inspection surface
reveals existing full-history evidence without waiting for a complete
goal/action/result/pending/state extractor. It must clearly disclose its narrower
meaning and must not recreate one Session or source pair per Workstream.

Delivered sequence and independent proposal:

1. Close the existing simulation's real replay gap and prepare its versioned,
   source-revalidated read contract: [FEAT-0101](../feature/feat-0101-full-history-affinity-inspection-contract.md).
2. Consume that passed contract in the existing Auto Work destination: overview
   → similarity neighborhood → exact source inspector and return:
   [FEAT-0102](../feature/feat-0102-full-history-affinity-map.md).
3. Independently assess one direct-context relation candidate:
   [FEAT-0103](../feature/feat-0103-direct-context-work-relation-trial.md).
   Its failure cannot block affinity inspection. Its PASS cannot automatically
   authorize private application, a map overlay or transitive flow identity.

The owner subsequently authorized continuous implementation through visible UI.
FEAT-0101 passed after actual replay verification; FEAT-0102 passed its original
scoped checks before its geometry was superseded by FEAT-0104. The temporal map
is available for optional observation. FEAT-0103 remains draft/unexecuted.
No further model trial, organization migration or cleanup ran. FEAT-0098/0100 remain blocked;
FEAT-0099 remains a passed supplied-input state projector, not an extractor.

### Information And Interaction Hierarchy

| Layer | Meaning and representation | What it cannot claim |
| --- | --- | --- |
| Whole-history affinity inspection | Coarse similarity group → subgroup/neighborhood; undirected affinity, representative source label, full totals and exclusions | enduring area, same effort, importance, active/completed state or causation |
| Later evaluated work relationship | Separately labeled direction and cited compatible outcomes/backward link, after a new real-context/consumer boundary | confirmation, automatic transitive identity, branch/merge or lifecycle from pair labels alone |
| Original evidence | Exact Session/message occurrence, speaker, time, revision and available original source | source authority for an unsupported model interpretation |

The first screen implements only affinity inspection and original evidence. It
does not show placeholder causal arrows or make a missing relation model look
like a broken feature. Broad groups are entry points, source nodes appear on
expansion, and all groups/singletons remain reachable. No forced cluster count,
top-N semantic cutoff or repeated approval queue conceals fragmentation.

Keep the selected anchor stable as its inspected scope expands. Pan/zoom/fit/reset,
keyboard, browser history, snapshot-scoped direct links, reduced motion and a
complete paged textual fallback reuse the current Explorer/Focus family. Observed
time can order inspected source history; it does not establish work lineage.
The old sampled comparator remains explicitly separate from full-history results.
Opening or expanding the map performs no model work, source ingestion or refresh.

### Inspection Batches And Exit Gates

| Batch | Goal and why grouped | Guardrails and targets | Validation and exit |
| --- | --- | --- | --- |
| 3E — Full-history read readiness | Keep source freshness, replay and consumer authority together so the map cannot guess its data semantics | FEAT-0101; existing simulation/cache owner, source locators and bounded reads; no UI or new inference | Real unchanged/grouping-only replay with zero encoding, delta/invalidation, full accounting and exact provenance; Contract/Functional PASS |
| 3F — Observation map | Deliver one coherent overview/neighborhood/source journey instead of three unrelated screens | FEAT-0102; existing Auto Work, source details and shell; no inferred work identity or mandatory classification | Running-browser evidence at 1440/920/700/320, input/history/focus/fallback checks and local readiness; all four evaluators PASS, not grouping-quality PASS |
| 3G — Direct-context relations | Isolate relation understanding from the failed role/state inventory producer | FEAT-0103; one installed-model synthetic candidate, host-owned source coordinates; no private or UI job | Frozen cohort gates and primary semantic review, failure-preserving replay and fixed stop; PASS leads only to a proposed real-context study |

3E passed; 3F's original technical result is historical and its presentation is
superseded. FEAT-0104/RUN-117 owns the passed temporal/continuous-canvas successor.
3G remains draft and unexecuted. These exit gates are not current execution orders.

This is a bounded pre-quality inspection exception, extending the earlier
FEAT-0096 precedent. It does not waive replacement quality, stable identity,
correction semantics or finite legacy transition. New affinity/readiness visual
state mappings must be reconciled with the Design Constitution before visible
implementation; this plan creates no separate tokens or durable design law.

## Approved Inspection Batch — 2026-09-16

The owner accepted [Auto Work inspection](../feature/feat-0096-auto-work-inspection.md)
before the quality verdict and placed it immediately above Workstreams in the
sidebar. This batch is `extend`, `fullstack-product`: preserve the shell, show
actual inferred groups and source reasons, expose unassigned/sample scope, and
require no approval queue. Its target is `/auto-work`, with backend preview then
frontend browse/read lanes. Validate synthetic wide/compact/narrow and keyboard/
history/no-script states plus private local readiness. Exit is a usable local
inspection URL, not replacement viability. Older quality-before-*any*-UI wording
below is superseded only for this batch; production replacement and legacy
transition still require their own evidence. No tokens or creative DNA change.

The inspection UI is implemented and verified under
[RUN-20260916-102](../run/run-20260916-102-auto-work-inspection.md). The next
human observation can use actual local output; semantic quality is still open.

## Purpose

- Reframe LocalBrain's editor-first Workstream experience into a source-backed,
  time-directed map that reveals how one effort developed, branched, received
  evidence, merged, or ended.
- Reconcile Quartz and Obsidian's approachable graph exploration with
  LocalBrain's stronger requirements for temporal direction, causal
  explanation, source authority, stable spatial memory, and light human
  correction.
- Retain the passed Session Focus as a supporting inspection surface. Validate
  automatic outcome-level reconstruction before replacement UI, then replace manual
  Workstream/Thread maintenance with an immediately usable overview and flow
  detail. Global Atlas rendering and terrain are later presentation choices.

## Plan Type

- Information-hierarchy reconciliation.
- Interaction and navigation consistency.
- Automatic work organization and replacement of the editor-first workspace,
  with explicit reconciliation of the current product hierarchy rules.

## Baseline

### Audited Sources

- [PRD-0017](../prd/prd-0017-source-backed-workflow-map-and-workstream-lenses.md):
  approved analysis/validation planning boundary; automatic organization and
  minimum confirmation supersede the candidate-inbox/promotion-first proposal.
  The first experiment Feature is approved and synthetically verified, but
  blocked on independent quality assessment after an unassessed current-data
  execution check; no viability or UI handoff follows from execution alone.
- [Quartz Graph View](https://quartz.jzhao.xyz/features/graph-view): local and
  global graph, one-hop focus, visited state, drag, and zoom.
- [Obsidian Graph View](https://obsidian.md/help/plugins/graph): local depth,
  filters, direction display, force controls, navigation, and chronological
  playback.
- Current `Workstream` detail: editor-first hierarchy of checkpoint, Runner,
  Threads, resources, Suggestions, and manual forms.
- Current primary `Session` detail: readable conversation plus bounded direct
  evidence and explicit-organization rail.
- Current `Schema` Explorer: visible zoom/reset/fit controls, pointer-anchored
  zoom, bounded canvas, deterministic fallback, focus, and history behavior.
- Current Sessions inventory and Project grouping: stable Session identity,
  source cue, branch metadata, activity time, pin, and bounded grouping.

### Constitution And Owner References

- [Design Constitution](../../policies/design/design-constitution.md)
- [Design Document Governance](../../policies/design/design-document-governance.md)
- [Design Evaluation](../../policies/design/design-evaluation.md)
- [Interaction Evaluation](../../policies/experience/interaction-evaluation.md)
- [Product Model](../../policies/project/product.md)
- [Privacy And Data Handling](../../policies/project/privacy-and-data.md)

### Source Reconciliation

| Source | Adopt | Adapt | Reject |
| --- | --- | --- | --- |
| Quartz | local/global scope, bounded neighborhood, hover/focus, pan/zoom | local scope becomes lineage plus evidence; global becomes bounded Atlas | degree as workflow importance, force position as meaning, every note as a peer node |
| Obsidian | local depth, filters, direction cue, keyboard navigation, chronological inspection | chronological playback becomes work-history scrubbing only if it improves reconstruction | continuous force motion, link tension as causal strength, flat vault-wide hairball |
| LocalBrain Schema | visible controls, fit/reset, modifier/pinch zoom, bounded overflow, fallback | reuse the interaction contract while selecting a renderer suitable for dynamic focus and branches | treating static Mermaid output as the predetermined Workflow Map renderer |
| Current Workstream | provenance, user-authored meaning, safe recovery, status context | replace manual organization with inferred flows and optional scoped correction; define legacy transition separately | form completion, candidate approval, or promotion as a prerequisite for useful work reconstruction |
| Current Session detail | Session reading, source provenance, exact evidence reasons | add a workflow entry without contaminating the authoritative related rail | mixing inferred workflow proximity into direct-reference groups |

### Product-Law Reconciliation

- The owner explicitly requested replacement of manual Workstream/Thread
  maintenance, not an additive candidate inbox. This changes the proposed
  information hierarchy and normal interaction path.
- Design Constitution Sections 3, 10, and 15 still describe user-defined
  organization and Workstream → Thread → evidence. They remain current
  implementation law. The replacement requires a reviewed update to those
  owners before visible implementation; this plan is not a second constitution
  and grants no exception to current runtime behavior.
- Keep provenance, optional user corrections, reversible consequences,
  accessibility, responsive behavior, and the shared shell/token system.
  Showing useful inferred flows without approval does not label them confirmed.
- No rendered screen, private corpus, model, or migration was evaluated during
  this planning update. Existing screenshots and passes cover only their
  historical surfaces.

## Scope

- Define one coherent visual grammar for `flow spine`, `evidence
  constellation`, and optional `topic terrain`.
- Define the semantic zoom relationship among `Atlas`, `Focus`, and `Trace`.
- Reuse passed Focus/Trace for source inspection. Whole-history affinity
  inspection follows its data-readiness contract; automatic flow quality remains
  a prerequisite for replacement overview/detail and exception correction, not
  for the narrower observation surface.
- Define stable selection, expansion, collapse, pan, zoom, fit, reset, direct
  entry, browser history, keyboard focus, reduced motion, and diagram fallback.
- Define how observed, inferred, explicit-organization, and user-confirmed
  relationships remain distinguishable without relying on color alone.
- Define how source identity, freshness, availability, lifecycle, activity,
  and epistemic authority coexist without collapsing into one visual state.
- Plan wide, compact, and narrow transformations at the existing `920` and
  `700` breakpoints and validate the supported `320` floor.
- Use synthetic representative histories for all tracked design artifacts and
  evaluation evidence.

## Non-Goals

- Writing Specs, selecting a renderer, or changing schema, queries, routes,
  templates, JavaScript, CSS, or runtime behavior before the selected child
  Feature passes human boundary review.
- Restyling the persistent LocalBrain shell or importing Quartz/Obsidian's
  product chrome, tokens, typography, force settings, or generic graph theme.
- Requiring an active Qwen/embedding runtime or generated summaries merely to
  view prepared evidence. The proposed affinity surface consumes existing
  admitted vectors; it does not run models for visual effects or navigation.
- A decorative global graph, freeform whiteboard, manual saved node positions,
  arbitrary relation drawing, or continuous background animation.
- Showing every source record, Activity Event, document, URL, or message as a
  peer node.
- Replacing Session reading, Atlassian Explorer, Local Context Explorer,
  Search, or Schema with the Workflow Map.
- Deciding the migration or removal of existing Threads in this design plan.
- Treating visit frequency, link degree, Session count, token volume, or visual
  centrality as work importance.

## Invariants

- Where time is displayed it has a stable direction; ordinary focus changes
  do not randomize positions. Affinity layout is not a causal time axis.
- The selected item remains the interaction anchor while its neighborhood
  expands. In admitted lineage views that item is an Episode; affinity groups
  do not inherit Episode identity or meaning.
- Delivered Focus remains useful without terrain or AI. Replacement screens
  must render admitted results and source fallbacks independently of an active
  model; extraction sufficiency remains the separate experiment's question.
- A source artifact stays subordinate to its admitted workflow line, or to its
  similarity group in affinity inspection. It does not become an Episode merely
  by being drawn as a node.
- Direct evidence, inferred relation, explicit organization, and user
  confirmation remain structurally and textually distinguishable.
- Routine entry and new-activity updates require zero flow/name/membership
  approvals. Uncertainty must not become a mandatory review backlog.
- Visible item budgets never define the number of actual flows or conceal
  fragmentation, missed goals, or unexamined evidence.
- Main/supporting presentation is relative to the selected Workstream lens and
  never becomes a global ranking of the user's work.
- Quietness and closure are independent. No inactivity treatment may look like
  confirmed completion.
- Source provenance color identifies origin only; selection, confidence,
  lifecycle, and priority retain separate non-color cues.
- Essential actions have visible controls and keyboard paths. Hover, glow,
  animation, wheel gestures, or force motion never carry essential meaning.
- Desktop canvas interaction does not create a nested-scroll trap when the map
  becomes sequential content at compact or narrow widths.
- A textual lineage and evidence path remains available when graphical
  rendering fails or is inappropriate.

## Findings

### F-01: A Document-Link Graph Does Not Explain Work Direction

- Quartz and Obsidian make connection density approachable, but their default
  node/edge model does not distinguish continuation, branch, contribution,
  merge, blockage, supersession, or closure.
- LocalBrain must make direction and relation meaning legible before visual
  density or global coverage.

### F-02: Force Layout Weakens Spatial Memory

- Repulsion and link tension help separate a static note graph but can relocate
  context during expansion and make the same history feel different across
  visits.
- Workflow placement should use a stable time axis and deterministic branch
  lanes. Motion may explain a local expansion but must settle without changing
  the remembered topology.

### F-03: Flat Source Nodes Recreate The Radial-Inventory Problem

- Sessions, Jira, Wiki, Local Contexts, Git, Slack, and Mail carry different
  authority and granularity.
- Treating all records as peer circles produces a source catalog rather than a
  story of work. The selected Episode owns a bounded evidence constellation;
  other artifacts remain collapsed until needed.

### F-04: The Current Workstream Surface Requires Organization Before Value

- Current checkpoint, Thread, link, and editing forms place the maintenance
  burden before the user can observe a coherent flow.
- The target composition leads with the derived story and current tips. Editing
  becomes a contextual correction or durable naming task.
- The old candidate inbox and repeated promotion proposal still left the user
  managing organization before normal use; it is superseded, not the next UI.

### F-05: Visual Richness Must Come From Supported Meaning

- Repeated circles and lines are visually monotonous, but decorative gradients,
  random physics, and constant animation would add spectacle without meaning.
- Temporal distance, branch geometry, activity ticks, terminal markers,
  evidence density, source glyphs, selection depth, and progressive reveal can
  create variation while remaining informative.
- Only admitted semantics may drive those treatments. The affinity increment
  can use observed time, evidence density and selection/reveal, not speculative
  branches, terminal states or importance.

### F-06: Global Coverage Needs A Focus Budget

- A complete cross-source graph will grow faster than a single readable
  canvas. Showing it all by default would reproduce the hairball problem even
  with a directional layout.
- Atlas should lead with bounded active/recent tips and named lenses. Focus
  should reveal only the selected lineage and a controlled neighborhood. Trace
  should own the detailed evidence.

### F-07: Reference Combinations Are Not Outcome-Level Flows

- A pair per top-level card can fragment one effort into dozens of candidates.
  A clean grid, pagination, or a top-N cap does not correct that semantic error.
- Validate goal coverage, incompatible-goal separation, fragmentation,
  correction burden, and stable updates before composing the replacement.
- Use subordinate activity for investigation/implementation/verification when
  they contribute to one outcome. Do not require manual Thread creation.

## Target Visual Grammar

The Flow Spine and Atlas state grammar below describe the destination after
relationship/state admission. They do not authorize those semantics in the
current affinity-only inspection batch; its hierarchy is defined above.

### Layer 1: Flow Spine

- Horizontal direction represents time at wide viewports; narrow presentation
  may become a top-to-bottom lineage while preserving relation direction.
- A stable trunk expresses the selected lens. Supporting, alternative, blocked,
  superseded, and merged paths use distinct geometry, endpoint markers, and
  readable relation labels rather than color alone.
- Meaningful temporal gaps may appear as compressed breaks or labeled distance;
  they do not imply a workflow ended.
- Activity ticks may summarize bounded source-backed work along a segment, but
  event volume does not change the line into an importance score.

### Layer 2: Evidence Constellation

- Selecting an Episode reveals a bounded ring or adjacent cluster of Session,
  Jira, Wiki, Local Context, Git, and future source artifacts.
- Each artifact keeps its own compact source cue, kind, availability/freshness,
  and reason for appearing. It links to the owning detail surface.
- The constellation expands without moving the selected Episode or rebuilding
  the whole map. Overflow collapses by source group with honest retained totals.

### Layer 3: Topic Terrain

- Topic terrain is an optional soft background region or contour. It may orient
  related paths but does not become a parent, branch, Workstream, or authority.
- The deterministic first boundary may derive only explicit Project, Jira,
  Workstream, or other named areas. If that evidence is weak, the terrain is
  omitted instead of replaced by an invented cluster.
- A later semantic model may propose terrain behind a separate provenance and
  evaluation boundary. The Flow Spine remains unchanged when that capability
  is unavailable.

### Semantic Zoom

| Scale | Primary question | Visible structure | Deferred detail |
| --- | --- | --- | --- |
| Atlas | What work is alive, quiet, or recently closed? | named Workstream lenses, bounded tips, broad branch shape, activity/closure state | individual evidence and most Episode labels |
| Focus | How did this effort reach its current point? | selected Episode, lineage, bounded branches/merges, tips, terminal markers, immediate evidence counts | full source contents and distant unrelated lines |
| Trace | Why is this node or edge here? | intent/outcome fields when supported, source-grouped evidence, relation reasons, freshness, authority, destinations | global topology |

## Target Interaction

### Click-To-Illuminate

1. Single selection locks one Episode as the focus anchor.
2. Its supported ancestors, descendants, and branch/merge path gain primary
   emphasis while unrelated context dims but remains locatable.
3. A bounded evidence constellation and relationship explanation become
   available only after the destination data is ready.
4. Expanding another branch preserves scale, selected position, canvas scroll,
   shell, and browser history orientation.
5. Opening a source destination retains a deterministic return path to the same
   map focus.

### Control Ownership

- Visible `zoom out`, `current scale/reset`, `zoom in`, and `fit` controls follow
  the existing technical-canvas family unless the approved Feature establishes
  a more specific reusable role.
- Modifier-wheel or pinch zoom preserves the pointer neighborhood. Ordinary
  wheel/trackpad motion remains page or bounded-canvas scrolling.
- Selection and correction actions belong to the selected Episode/edge
  inspector, not to ambiguous blank canvas space.
- An inferred relation exposes `inspect reason` before any correction. A user
  action states whether it confirms continuity, creates a split assertion,
  merges into a known line, closes with a reason, or reopens a tip.

## Planned Work

### Batch 1: Source And State Storyboards

- Goal: lock the minimum visual states before selecting a renderer or creating
  a product Feature.
- Why this grouping: source authority, relation authority, activity, lifecycle,
  and selection must remain independent or every later screen will encode
  ambiguous state.
- Guardrails: synthetic data only; no new token, state, route, schema, or
  model assumption.
- Targets: observed origin, open tip, quiet tip, completed tip, merged branch,
  inferred edge, confirmed edge, abstained Session, stale evidence, unavailable
  evidence, and diagram failure.
- Validation: side-by-side information-hierarchy review against the
  constitution and PRD-0017 source map.
- Exit gate: every state can be explained without color, motion, or opening a
  source detail.
- Proposed Feature owners:
  [FEAT-0085](../feature/feat-0085-workflow-episode-and-direction-contract.md)
  and
  [FEAT-0086](../feature/feat-0086-deterministic-cross-source-workflow-projection.md).

### Batch 2: Focus Interaction Prototype

- Implementation state: `passed`. RUN-20260914-97 delivered the deterministic
  Session entry, fixed wide/compact map, narrow and no-script lineage,
  selected-path illumination, bounded branch disclosure, adjacent Trace,
  browser history, and zoom/fit controls. Contract, Design, Functional, and UX
  Heuristic evaluations all passed.

- Goal: validate the tree-like local map and click-to-illuminate behavior on one
  representative branching and merging history.
- Why this grouping: Focus is the smallest surface that can prove the product
  insight before a global Atlas multiplies density.
- Guardrails: deterministic synthetic projection; fixed node positions; bounded
  neighborhood; no topic terrain or correction persistence required.
- Targets: selected Episode, lineage, collapsed branch counts, evidence
  constellation, relation inspector, pan/zoom/fit/reset, direct entry, return,
  and textual fallback.
- Validation: wide/narrow rendered comparison, keyboard traversal, reduced
  motion, pointer-anchor preservation, branch expansion stability, and source
  explanation review.
- Exit gate: the user can answer what led here, what diverged, what remains
  open, and why each visible relation exists without reading full Sessions.
- Proposed Feature owner:
  [FEAT-0087](../feature/feat-0087-session-workflow-focus-map.md).

### Batch 3A: Workflow Boundary Correction

- Implementation state: `passed`. RUN-20260914-98 delivered the
  append-only assertion foundation and RUN-20260914-99 delivered contextual
  preview, correction, undo/reopen, stable orientation restoration, inline
  recovery, no-script forms, and four-width containment. Contract, Design,
  Functional, and UX Heuristic evaluations passed.

- Goal: place bounded same/split/merge/close/reopen correction into the Focus
  experience without returning to an editor-first form wall.
- Why this grouping: product controls must consume an approved user-assertion
  contract and should not be simulated as visual behavior before their
  consequences are known.
- Guardrails: corrections never rewrite source evidence; one primary action per
  action group; failure preserves prior map state; existing organization stays
  visible during any migration period.
- Targets: inspector actions, consequence preview, confirmation/provenance cue,
  undo/reopen, focus/scroll preservation, and current Workstream regression
  safety.
- Validation: normal/failure/no-script paths, direct source return, repeated
  corrections, stale projection handling, and existing Workstream regressions.
- Exit gate: consequential boundaries can be corrected in a few contextual
  actions without manually recreating the graph.
- Proposed correction owners:
  [FEAT-0088](../feature/feat-0088-workflow-assertion-and-correction-contract.md)
  and
  [FEAT-0089](../feature/feat-0089-workflow-boundary-corrections.md).

### Batch 3B: Reconstruction Quality Before Replacement UI

- Planning state: FEAT-0095 is approved and implemented; RUN-20260915-101 is
  blocked on independent quality assessment after synthetic technical PASS and
  unassessed current-data execution. Source/time selection is resolved.
  The former candidate chain is superseded;
  FEAT-0090 remains a tested comparator only.
- Goal: establish whether outcome-level flows can be reconstructed with low
  fragmentation and correction burden before designing their primary surface.
- Why this grouping: source eligibility and grouping quality determine the
  visible work unit; design must not conceal poor extraction behind a short list.
- Guardrails: no private-data experiment, body read, model, prototype, or
  replacement Feature execution from PRD approval alone. The bounded experiment
  needs Feature approval and explicit local inputs. Use synthetic tracked
  artifacts only; private review stays local, outside hosted agents.
- Targets: coherent outcome, subordinate contribution, mixed Sessions,
  changing references, unfamiliar/unassigned evidence, count and coverage,
  incremental stability, and zero routine confirmations.
- Validation: [PRD-0017's reconstruction plan](../prd/prd-0017-source-backed-workflow-map-and-workstream-lenses.md#reconstruction-validation-plan)
  owns the upper comparison/scenario/quality envelope; FEAT-0095 fixes
  executable input limits, scoring, and technical-versus-viability outcomes.
  Source-only planning review or synthetic-only evidence is not local viability.
- Exit gate: reviewed evidence establishes a viable work unit and correction
  burden. A failure returns to extraction/admission, not to a candidate inbox.
- Feature owner: [FEAT-0095: Bounded Work Reconstruction Experiment](../feature/feat-0095-bounded-work-reconstruction-experiment.md)
  (`blocked`; Contract and Functional technical PASS with partial evidence;
  no visible surface).

### Batch 3C: Automatic Overview, Flow Detail, And Exception Correction

- Goal: make current work recognizable and resumable with no manual container,
  Thread, candidate approval, or promotion step.
- Why this grouping: overview, one-flow detail, source return, and optional
  correction must share one automatically maintained identity and state owner.
- Guardrails: quality/identity/update foundations pass first; inferred versus
  corrected state stays explicit; no hidden source/model operation at page
  entry; update product hierarchy and constitution owners before implementation.
- Targets: automatically named efforts, purpose and latest supported activity,
  subordinate work, current/quiet/closed/unknown distinctions, source evidence,
  optional scoped corrections, and stable incremental return/selection.
- Validation: zero-organization entry, repeated admitted data updates without
  approval, misleading-group correction and undo, honest unassigned coverage,
  responsive/no-script navigation, and shared source-detail regressions.
- Exit gate: users can recognize and resume work without maintaining a parallel
  candidate list. Actual flow counts and quality remain inspectable separately
  from the current-view display budget.
- Feature owner: proposed only after Batch 3B's complete viability evidence.

### Batch 3D: Controlled Legacy Experience Transition

- Goal: retire manual organization as the primary experience without losing
  still-needed user data, with finite transition recovery followed by retirement
  of obsolete storage rather than permanent preservation of the legacy model.
- Why this grouping: old entry points, data ownership, rollback, and expiry
  must agree when a replacement becomes the default.
- Guardrails: no deletion under this planning track or inferred legacy reassignment;
  the approved [transition boundary](../prd/prd-0017-source-backed-workflow-map-and-workstream-lenses.md#legacy-transition-boundary)
  must fix retention and cleanup scope before cutover. Temporary coexistence
  requires neither double maintenance nor per-record cleanup confirmation.
- Targets: old Workstream/Thread deep links, unmapped/conflicting records,
  time-bounded legacy access/rollback, new default entry, correction carryover,
  and retirement of eligible unused/orphaned data, tables, and expired backups.
- Validation: synthetic existing-data migration/recovery witnesses, old/new
  direct-entry and post-retirement states, still-needed text/relationship
  transfer, shared-owner protection, retention expiry, remaining-data integrity,
  actual storage reclamation, and removal of duplicate required maintenance.
- Exit gate: the automatic experience is primary, required data has a new owner,
  and agreed retention/cleanup is completed with a minimal record. Legacy
  recovery and full copies are not permanent requirements.

### Batch 4: Atlas And Trace Reconciliation

- Planning state: optional global-map presentation after the automatic overview
  and flow detail; not a gate on their basic usability.
- Goal: extend the validated Focus grammar into an overview and detailed source
  explanation without creating three unrelated screen families.
- Why this grouping: global density and detailed evidence have opposite
  information pressures and should consume a proven middle scale.
- Guardrails: Atlas is bounded to meaningful named/active/recent context; Trace
  keeps source authorities separate; persistent shell remains unchanged.
- Targets: Atlas tips and lenses, Focus handoff, Trace inspector/full detail,
  filters, browser history, empty state, and unavailable source combinations.
- Validation: multi-scale navigation continuity, direct entry, back/forward,
  long histories, long labels, and source-family combinations.
- Exit gate: Atlas → Focus → Trace and the return path feel like one continuous
  product rather than dashboard, graph, and detail fragments.

### Batch 5: Optional Terrain Evaluation

- Goal: determine whether a topic background improves orientation after the
  deterministic map is already useful.
- Why this grouping: semantic terrain should solve an observed navigation gap,
  not justify a model dependency or add visual novelty.
- Guardrails: no model admission inside this design batch; explicit deterministic
  areas first; terrain never changes flow relations or user assertions.
- Targets: no-terrain baseline, explicit-area terrain, ambiguous overlap,
  unavailable semantic capability, and candidate provenance.
- Validation: compare orientation, false grouping, label burden, visual noise,
  runtime cost, and correction burden against the same frozen histories.
- Exit gate: either terrain earns a later bounded model evaluation or the
  product records that Flow Spine and evidence are sufficient.

## Validation Gates

- FEAT-0101/0104 have passed; FEAT-0102's freeform presentation is superseded and
  FEAT-0103 remains an independent proposal. Their dependency order and narrow
  outcomes govern the inspection track; earlier replacement gates below
  cannot silently turn into a prohibition on all pre-quality observation.
- FEAT-0085 through FEAT-0089 remain passed. PRD-0017 and Batch 3B's FEAT-0095
  boundary are approved, but the experiment's missing local evidence blocks
  later replacement/transition work, which still needs its own approved boundaries.
  Superseded FEAT-0091–0094
  must not supply implementation authority.
- Extraction quality and correction-burden gates pass before replacement UI.
  Visual polish or a short display list cannot stand in for those results.
- Required foundation Features pass before product prototypes expose data or
  correction behavior that depends on them.
- Visual review compares Quartz/Obsidian reference contributions without
  importing their graph semantics or appearance as product law.
- Wide and responsive evidence covers `1440`, `920`, `700`, and `320`, plus
  long Korean/English/mixed labels and representative large bounded histories.
- Selection, focus, browser history, canvas scroll, expansion, and return state
  remain stable across click, keyboard, direct entry, back/forward, and
  breakpoint changes.
- Visible zoom/reset/fit and keyboard access pass; modifier/pinch zoom does not
  steal ordinary page scroll.
- Reduced motion retains all relation, selection, lifecycle, and expansion
  meaning.
- Direct, inferred, explicit-organization, and user-confirmed relations remain
  distinguishable without color alone and include an inspectable reason.
- Stale, unavailable, abstained, empty, error, render-failure, and no-script
  states preserve a usable source-backed path.
- No tracked artifact contains private runtime titles, paths, ticket keys,
  excerpts, source inventories, or screenshots.

## Risks / Open Questions

- Existing communities may remain fragmented or dominated by one Session even
  after the whole corpus is visible. Show that evidence; do not relabel renderer
  completion as coherent work organization or fix it by hiding groups.
- Membership-derived group IDs can move after recomputation. Snapshot-scoped
  navigation protects interpretation but is not durable work-area identity.
- The relation trial tests supplied context pairs, not candidate retrieval or
  whole-history flow assembly. Its success cannot eliminate those later gates.
- A time-directed layout may still become too wide for long Workstreams; the
  compression and branch-budget rules remain open.
- The passed Focus uses a fixed route-scoped DOM/SVG renderer. Whether a later
  Workstream Lens or Atlas can extend it while preserving stable incremental
  layout remains open and belongs to that future interaction contract.
- An overview can become an activity dashboard or importance ranking if its
  automatic flows are not outcome-level or its current/quiet/lifecycle states
  are conflated. User promotion must not be introduced as a cure for this gap.
- Evidence constellations can reproduce the hairball if source grouping,
  disclosure, and retained-total budgets are not explicit.
- A visual `main` line may appear globally authoritative unless the selected
  Workstream lens and outcome remain continuously visible.
- Existing Workstream forms and inferred flows may duplicate authority during
  transition. Distinct provenance and a bounded cutover are needed; manual
  Workstream/Thread maintenance is not an accepted permanent requirement.
  Legacy rows, tables, and backups must not become permanent storage debt;
  retention duration and exact retirement scope remain transition decisions.
- Topic terrain may be unnecessary, visually noisy, or mistaken for confirmed
  organization. It remains optional and follows Focus validation.
- Existing correction controls passed for Episode/edge assertions only. Their
  lifecycle may inform, but does not define, automatic flow identity or legacy
  migration. Outcome extraction and correction burden remain unverified.

## Exit Goal

- One approved, evidence-backed experience automatically organizes and updates
  outcome-level work, supports direct resumption and source inspection, and
  needs correction only as an exception. It replaces required Workstream/Thread
  maintenance while retaining still-needed user meaning and provenance, with
  bounded legacy recovery and completed obsolete-storage cleanup. Model
  sufficiency is decided by the separate extraction experiment, not visual law.

## Handoff To Next Track

- Current handoff: the owner stopped further development and calculations on
  `2026-09-24` after inspecting FEAT-0104's continuous time map. Keep the delivered
  map available; this is not a request to remove it or to complete another trial.
  Documentation review, commit and push do not resume product work. A later
  owner request is the resume trigger; no classification or approval queue is due.
  FEAT-0101's source reader remains passed; FEAT-0102's freeform UI is superseded. FEAT-0103
  remains a separate bounded proposal, not a UI prerequisite or an active trial.
  The earlier continuous-through-UI authorization delivered RUN-113–117 and is
  no longer an instruction to continue. Semantic work identity, automatic update,
  replacement and finite legacy-transition gates remain open.
- FEAT-0085 through FEAT-0089 and their dependency-ordered Runs are complete.
  Product review retains their deterministic Focus and correction path as a
  subordinate Session Lineage/Trace layer. FEAT-0090 retains only its isolated
  technical result; RUN-20260915-100 returned to planning and FEAT-0091–0094 are
  superseded. FEAT-0095's single non-model comparison is implemented with
  synthetic and unassessed current-data execution evidence. RUN-20260915-101 is
  blocked on independent quality assessment; that partial result cannot authorize
  replacement. The separately bounded inspection exceptions above are not a
  candidate inbox and do not reopen unchanged product direction.
- Keep production Atlas state semantics, topic terrain and automatic causal
  overlays outside the affinity inspection implementation. A group overview is
  not an admitted global workflow model.
- Reusable user-confirmed structure and contextual-correction law is promoted
  to the Design Constitution; unresolved multi-scale choices remain in this
  plan.

## Continuity Notes

- `2026-09-23`: the owner's continuous-through-UI approval activated dependency-
  ordered RUN-113–115. Actual replay, passive read contract and full-history map
  pass. Applied screen-alignment extend with native shell/Trace, settled SVG,
  selection-preserving zoom and complete fallback; constitution v21 owns new
  derived states. Synthetic browser gates and private-safe live readiness pass.
  No relation trial, replacement-quality assertion or legacy retirement ran.

- `2026-09-23`: extended this existing design plan with batches 3E–3G rather than
  creating a parallel visual system. Decoupled whole-history affinity inspection
  from detailed extraction and a single-candidate relation trial. Prior failures,
  replacement gates and finite legacy retirement remain intact. Planning only;
  no private input, model execution, rendered UI or constitution change.
- `2026-09-14`: activated after PRD-0017 approval. Linked the dependency-ordered
  FEAT-0085 through FEAT-0089 proposals and retained renderer, Atlas, lens, and
  optional-terrain decisions outside the first Feature review.
- `2026-09-14`: Batch 2 Focus and the correction portion of Batch 3 passed
  FEAT-0085 through FEAT-0089. Workstream Lens, Atlas, and optional terrain stay
  active planning questions rather than implied implementation work.
- `2026-09-15`: owner review classified the delivered Session graph as useful
  but insufficient for the intended Workstream replacement. Batch 3 was split
  so completed correction no longer shares status with the returned Workstream
  candidate and Lens planning boundary.
- `2026-09-15`: linked the five proposed candidate Features and their separate
  usefulness review. This routing update approves no visual implementation;
  Lens composition and Atlas remain deferred product boundaries.
- `2026-09-15`: FEAT-0090 passed its non-visual contract Run. Updated routing to
  FEAT-0091 boundary review without approving projection, UI, Lens, or Atlas.
- `2026-09-15`: owner rejected excessive pair candidates and routine review/
  promotion. Replaced Batch 3B with a pre-UI reconstruction quality gate and
  added automatic overview/detail plus controlled legacy transition batches.
  Current constitution hierarchy changes remain pending; this planning update
  changes no runtime, visual tokens, private data, or approved execution target.
- `2026-09-15`: owner clarified finite legacy retention followed by disposal.
  Batch 3D now includes expiry, obsolete-storage cleanup, and post-retirement
  validation; preserving every old record or recovery copy forever is not an
  exit requirement. No cleanup or cutover was executed.
- `2026-09-15`: owner approved the PRD's bounded analysis/validation direction.
  Connected Batch 3B to draft FEAT-0095, including real extraction/comparison,
  local-only private review, and separate technical/viability outcomes. No
  prototype, runtime analysis, model, visible surface, or migration was started.
- `2026-09-15`: FEAT-0095 was approved and implemented with synthetic technical
  evidence. Updated Batch 3B routing to RUN-20260915-101's missing-private-input
  block; no rendered surface, design constitution, or transition behavior changed.
- `2026-09-16`: current-data scope was resolved and unassessed local execution
  passed. Updated the remaining gate to independent quality assessment only;
  no DB/period question or new visual implementation follows from this update.
