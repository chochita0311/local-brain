# RUN-20260915-101: Bounded Work Reconstruction Experiment

## Metadata

- ID: `run-20260915-101`
- Status: `blocked`
- Feature: [FEAT-0095](../feature/feat-0095-bounded-work-reconstruction-experiment.md)
- Parent PRD: [PRD-0017](../prd/prd-0017-source-backed-workflow-map-and-workstream-lenses.md)
- Active Spec: [SPEC-0095](../spec/spec-0095-bounded-work-reconstruction-experiment.md)
- Surface: `mixed` (`data`, `infra`)
- Execution Profile: `foundation-contract`
- Required Evaluators: `contract`, `functional`
- Created: `2026-09-15`
- Updated: `2026-09-16`

## Goal And Authority

The owner approved the single bounded experiment after Feature review. Build
the minimal non-model comparison, synthetic checks, and explicit read-only
local adapter. No UI, production persistence, model, migration, or legacy
cleanup is authorized. Private content must never enter the hosted agent.

## Selected Loop

- Orchestrator: Feature boundary accepted; foundation profile, no visual lane.
- Lane order: extraction/scoring, then local experiment adapter/command.
- Builder and final semantic evaluation: primary agent.
- Spec readiness: experimental state only; no production ownership ambiguity.
- Current phase: bounded preparation and unassessed current-data execution
  verified; independent quality assessment remains unavailable.
- Attempt: 2 (local unblock; original technical evidence remains historical).

## Evidence And Current Route

Owner follow-up on `2026-09-16` approved the separate
[Auto Work inspection Run](run-20260916-102-auto-work-inspection.md) so actual
output can be viewed before a quality verdict. This Run remains blocked on
independent quality evidence; its earlier no-UI boundary applies to the
comparison implementation, not the subsequently approved inspection increment.

- Technical result: PASS for the implemented isolated boundary; `61/61` focused
  and `645/645` total tests passed. Both required evaluations have `partial`
  Feature coverage, not a reconstruction viability result.
- Required viability evidence: independent local expectations and assessed
  quality. Current-data scope was resolved and an unassessed local comparison
  ran successfully; its unknown placeholders are not an answer key. Authored
  linguistic witnesses do not replace blinded or real-data quality assessment.
- Next role: independent local assessment preparation and evaluator
  reconciliation under the already-approved Feature, not more DB/period questions.
  Do not ask the owner to name or assign the whole sample; at most the approved
  one batch of five consequential ambiguities may be reviewed. Unknowns stay
  unknown if independent expectations cannot be prepared within that boundary.
- The owner named current LocalBrain data through now. Configuration-based path
  resolution is now authorized for that target; no path or period question is due.

## Current Artifacts

- Spec: SPEC-0095.
- Evaluator reports:
  [Contract](../evaluation/eval-0095-contract-bounded-work-reconstruction-experiment.md),
  [Functional](../evaluation/eval-0095-functional-bounded-work-reconstruction-experiment.md).
- Fix log: none.
- Temporary runtime artifacts: none retained.

## Implementation And Verification

- Implemented explicit-goal extraction and experimental grouping, metadata-only
  pair comparison, independent scoring/replay, bounded read-only input, and a
  supervised local command. No app entry, route, table, scheduler, model, source,
  correction ledger, or Workstream/Thread mutation was added.
- Scope exclusions precede body access. Synthetic real-schema and database-mode
  CLI tests verify source bytes unchanged, admission hashes/time bounds,
  canonical references, fixed default output, owner-only new local results,
  cancellation/limit reaping, and failure cleanup.
- Post-contract regression: PASS (`645/645`); existing consumers remain unchanged.
- Generated schema cleanup ledger: current (`649` objects, `keep=543`,
  `defer=106`, no changed/removed objects). Refreshed runtime SQL-use references
  only; preserved prior audit decisions and schema.
- Final repository hygiene: artifact catalog built and checked; privacy scan
  passed for `918` candidate files; `git diff --check` passed. Validated `1,549`
  local Markdown links across `26` changed documents. `handoff-tmp.md` is absent
  and no durable output depends on it. No task-owned temporary artifact is retained.
- Human review now: none for database selection or time range. The owner already
  chose current stored LocalBrain information through now. Independent assessment
  remains an evidence/preparation gap; do not turn it into whole-sample manual
  classification or use either arm's predictions as the answer key.
- Attempt 2 processed admitted indexed text only inside the local supervised
  command. No private content, identities, labels, sample counts, source times,
  or detailed reports entered hosted-agent output. Product/Architecture/Privacy and
  planning routing were updated without changing the current manual runtime
  law. Finite legacy retention/retirement remains later approved transition work.

## Attempt 2 Current-Data Execution Receipt

- Source/time authority: existing configured LocalBrain database, entire
  retained eligible history through invocation time. Metadata-only preflight
  resolved the database/schema and confirmed that sampling was required.
- Added deterministic chronological preparation, three-cutoff replay where
  available, source-bound manifest hashes, explicit omissions, and unknown
  assessment placeholders. Source/topic words and predictions never select a
  sample or generate expected goals. Original experiment caps are unchanged.
- Actual local command returned `EXPERIMENT_RECORDED`. The local wrapper checked
  `assessment=unassessed`, `private_quality=insufficient`, and
  `viability=unverified`, then emitted only `UNASSESSED_RESULT_VERIFIED`.
  These are execution evidence, not actual accuracy or correction-burden scores.
- The temporary manifest/report served only this bounded execution check.
  The task-owned private directory was removed immediately afterward; the
  wrapper confirmed `TASK_PRIVATE_ARTIFACTS_REMOVED`. No private artifact is
  retained, and no lasting output depends on a temporary path.
- No source synchronization, body export to the hosted agent, model call,
  migration, or legacy modification occurred. Current-data path/time selection
  is no longer a blocker. The Feature/Run remain blocked only on independent
  quality evidence; no production/UI handoff follows from an unassessed run.

## Continuity Notes

- `2026-09-15`: started from explicit owner approval of FEAT-0095. Earlier
  RUN-20260915-100 remains returned-to-planning; its PASS is not reused as
  reconstruction evidence.
- `2026-09-15`: completed safe implementation and synthetic evaluation, then
  marked the Feature/Run blocked on missing private validation inputs. This
  boundary does not authorize production foundations, UI, a model, or cleanup.
- `2026-09-16`: owner resolved the source/time inputs as current LocalBrain data
  through now. Metadata-only read-only preflight found the configured database
  and required schema, with source count within the cap and bounded Session
  sampling required. No private names, counts, timestamps, or bodies were emitted.
  Resumed as attempt 2; independent expectations remain a separate quality gap.
- `2026-09-16`: bounded current-data execution succeeded with assessment explicitly
  unknown. Replaced the missing-DB/period route with independent quality
  assessment, retained partial acceptance, and removed task-private artifacts.
