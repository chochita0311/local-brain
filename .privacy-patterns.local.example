# Optional local-only literal denylist for names, project titles, URLs, and other
# private phrases that generic secret detection cannot identify. Matching is
# case-insensitive against whole words or phrases.
#
# Create `.privacy-patterns.local`, one literal phrase per line. Blank lines and
# lines starting with `#` are ignored. The local file is excluded from Git.
